---
phase: 25-next-js-scaffold-summary-view
plan: 04
type: execute
wave: 3
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - web/src/components/nav/date-list.tsx
  - web/src/components/nav/date-group.tsx
  - web/src/components/nav/date-picker.tsx
  - web/src/components/nav/nav-header.tsx
  - web/src/components/layout/left-nav.tsx
  - web/src/app/page.tsx
autonomous: true
requirements:
  - SUM-02
  - SUM-04
  - NAV-01

must_haves:
  truths:
    - "Left nav shows a scrollable date list organized into Daily, Weekly, Monthly expandable groups"
    - "Each daily date entry shows date, meeting count, commitment count, and theme tags"
    - "Prev/next arrows navigate to the next available date, skipping gaps"
    - "Calendar popover allows jumping to a specific date, with dates that have summaries highlighted"
    - "Unavailable dates show empty state with message like 'No summary for April 5'"
    - "Weekly and monthly entries are listed in their respective nav groups"
    - "Clicking a date/week/month entry updates the center panel content"
  artifacts:
    - path: "web/src/components/nav/date-list.tsx"
      provides: "Individual date entry with stats and theme tags"
    - path: "web/src/components/nav/date-group.tsx"
      provides: "Expandable group (Daily/Weekly/Monthly) in left nav"
    - path: "web/src/components/nav/date-picker.tsx"
      provides: "Calendar popover for date jumping"
    - path: "web/src/components/nav/nav-header.tsx"
      provides: "Prev/next arrows and calendar popover trigger"
  key_links:
    - from: "web/src/components/nav/date-list.tsx"
      to: "web/src/hooks/use-summaries.ts"
      via: "useSummaryList, useWeeklyList, useMonthlyList hooks"
      pattern: "useSummaryList|useWeeklyList|useMonthlyList"
    - from: "web/src/app/page.tsx"
      to: "web/src/components/nav/date-group.tsx"
      via: "renders nav groups in left nav"
      pattern: "<DateGroup"
---

<objective>
Build date navigation: left nav with grouped date lists (Daily/Weekly/Monthly), prev/next arrows, and calendar date picker.

Purpose: SUM-02 requires date navigation with prev/next and date picker. SUM-04 requires weekly/monthly browsing. This completes the navigation experience.
Output: Full date navigation in left sidebar with three grouped sections and a calendar popover.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-next-js-scaffold-summary-view/25-RESEARCH.md
@.planning/phases/25-next-js-scaffold-summary-view/25-CONTEXT.md

<interfaces>
From web/src/hooks/use-summaries.ts (Plan 01):
```typescript
export function useSummaryList(): UseQueryResult<SummaryListItem[]>
export function useWeeklyList(): UseQueryResult<WeeklyRollupListItem[]>
export function useMonthlyList(): UseQueryResult<MonthlyRollupListItem[]>
```

From web/src/lib/types.ts (Plan 01):
```typescript
interface SummaryListItem { date: string; meeting_count: number | null; commitment_count: number | null; has_sidecar: boolean }
interface WeeklyRollupListItem { week_label: string; year: number; week_number: number; start_date: string; end_date: string; daily_count: number }
interface MonthlyRollupListItem { month_label: string; year: number; month: number }
```

From web/src/stores/ui-store.ts (Plan 01):
```typescript
expandedNavGroups: Record<string, boolean>  // keyed by "daily", "weekly", "monthly"
toggleNavGroup: (id: string) => void  // Note: may be named differently, check actual store
```

From web/src/components/layout/left-nav.tsx (Plan 02):
```typescript
// LeftNav accepts onCollapse and children
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build date entry, group, and picker components</name>
  <files>
    web/src/components/nav/date-list.tsx
    web/src/components/nav/date-group.tsx
    web/src/components/nav/date-picker.tsx
    web/src/components/nav/nav-header.tsx
  </files>
  <action>
    IMPORTANT: Read web/CLAUDE.md and follow its instructions.

    First, install shadcn components needed:
    ```bash
    cd web && pnpm dlx shadcn@latest add calendar popover button
    ```
    Note: button may already exist, the command handles that gracefully.

    1. Create `web/src/components/nav/date-list.tsx` — "use client":
       - Component: `DateListItem` — a single date entry in the nav
       - Props: `date: string`, `meetingCount?: number | null`, `commitmentCount?: number | null`, `isSelected: boolean`, `onClick: () => void`
       - Render: Date formatted with date-fns (`format(parseISO(date), "EEE, MMM d")` for daily, e.g., "Mon, Apr 7")
       - Below date: small stats row — meeting count with `Video` icon, commitment count with `CheckSquare` icon (only shown if non-null and > 0)
       - Selected state: `bg-accent text-accent-foreground` background
       - Hover state: `hover:bg-accent/50`
       - Padding: `px-3 py-2 rounded-md cursor-pointer text-sm`
       - Transition: `transition-colors`

       Also export `WeeklyListItem` — Props: `weekLabel: string`, `dailyCount: number`, `isSelected: boolean`, `onClick: () => void`
       - Render: week_label text (e.g., "Week 14, 2026"), daily_count as subtitle ("5 daily summaries")
       - Same selection/hover styling as DateListItem

       Also export `MonthlyListItem` — Props: `monthLabel: string`, `isSelected: boolean`, `onClick: () => void`
       - Render: month_label text (e.g., "April 2026")
       - Same selection/hover styling

    2. Create `web/src/components/nav/date-group.tsx` — "use client":
       - Component: `DateGroup` — an expandable section (Daily, Weekly, Monthly)
       - Props: `title: string`, `icon: LucideIcon`, `count: number`, `groupId: string`, `children: ReactNode`
       - Use Zustand `expandedNavGroups[groupId]` for expand/collapse state (default to expanded)
       - Header: icon + title + count badge + ChevronDown/ChevronRight toggle
       - Header styling: `flex items-center gap-2 px-3 py-2 text-xs uppercase tracking-wider text-muted-foreground cursor-pointer hover:text-foreground transition-colors`
       - Count: small `Badge` with variant="secondary" showing number of items
       - Body: animated expand/collapse (use shadcn Collapsible or simple conditional render with `overflow-hidden` transition)
       - Notion-style feel: clean, minimal, clear visual hierarchy
       - Icons: `Calendar` for Daily, `CalendarRange` for Weekly, `CalendarDays` for Monthly

    3. Create `web/src/components/nav/date-picker.tsx` — "use client":
       - Uses shadcn Popover + Calendar components
       - Props: `availableDates: string[]` (ISO date strings), `selectedDate: string | null`, `onSelect: (date: string) => void`
       - Calendar trigger: small calendar icon button
       - In the Calendar, dates that have summaries are highlighted (use the `modifiers` and `modifiersStyles` or equivalent Calendar props to highlight available dates with a dot or accent color)
       - On date select: call `onSelect` with ISO date string, close popover
       - If selected date has no summary, the page will show empty state (handled by SummaryView)

    4. Create `web/src/components/nav/nav-header.tsx` — "use client":
       - Props: `selectedDate: string | null`, `availableDates: string[]`, `onDateChange: (date: string) => void`
       - Renders: prev arrow | current date display | next arrow | calendar picker icon
       - Prev/next arrows use `ChevronLeft` / `ChevronRight` Lucide icons
       - Prev: find the next available date BEFORE selectedDate in availableDates array. Disabled if at oldest.
       - Next: find the next available date AFTER selectedDate. Disabled if at newest.
       - Current date display: formatted as "April 7, 2026" using date-fns `format`
       - Calendar icon triggers the DatePicker popover
       - Styling: `flex items-center justify-between px-3 py-3 border-b`
       - Disabled arrows: `opacity-30 cursor-not-allowed`, enabled: `hover:bg-accent rounded-md p-1 cursor-pointer`
  </action>
  <verify>
    cd web && pnpm exec tsc --noEmit 2>&1 | head -20
  </verify>
  <done>Nav components type-check. DateListItem shows stats, DateGroup is expandable, DatePicker highlights available dates, NavHeader has prev/next arrows.</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation into left nav and connect to page state</name>
  <files>
    web/src/components/layout/left-nav.tsx
    web/src/app/page.tsx
  </files>
  <action>
    1. Update `web/src/components/layout/left-nav.tsx`:
       - Replace placeholder content with structured nav:
       - Props update: `selectedDate: string | null`, `selectedType: "daily" | "weekly" | "monthly"`, `selectedKey: string | null`, `onSelectDaily: (date: string) => void`, `onSelectWeekly: (year: number, week: number) => void`, `onSelectMonthly: (year: number, month: number) => void`, `onCollapse: () => void`
       - Header area: NavHeader with prev/next arrows and calendar picker (only for daily navigation)
       - Body: Three DateGroup sections stacked:
         - **Daily** (icon: Calendar): Map `useSummaryList()` data to `DateListItem` components, most recent first
         - **Weekly** (icon: CalendarRange): Map `useWeeklyList()` data to `WeeklyListItem` components
         - **Monthly** (icon: CalendarDays): Map `useMonthlyList()` data to `MonthlyListItem` components
       - Each group shows loading skeleton (3 shimmer rows) while its data loads
       - Each group hides entirely if its data is empty (no weekly files = no Weekly group shown)
       - Selected item highlighted based on selectedDate/selectedKey/selectedType

    2. Update `web/src/app/page.tsx`:
       - Add state management for selection:
         - `selectedType: "daily" | "weekly" | "monthly"` (default "daily")
         - `selectedDate: string | null` (for daily)
         - `selectedWeekly: { year: number; week: number } | null` (for weekly)
         - `selectedMonthly: { year: number; month: number } | null` (for monthly)
       - On mount: auto-select most recent daily summary from `useSummaryList()` data
       - Pass selection handlers to LeftNav:
         - `onSelectDaily`: sets selectedType="daily", selectedDate, clears others
         - `onSelectWeekly`: sets selectedType="weekly", selectedWeekly, clears others
         - `onSelectMonthly`: sets selectedType="monthly", selectedMonthly, clears others
       - Center panel routing:
         - If selectedType="daily" and selectedDate: render `<SummaryView selectedDate={selectedDate} />`
         - If selectedType="weekly" and selectedWeekly: render a simple `WeeklyView` that uses `useWeeklyRollup()` hook, shows markdown (reuse MarkdownRenderer)
         - If selectedType="monthly" and selectedMonthly: render a simple `MonthlyView` that uses `useMonthlyRollup()` hook, shows markdown (reuse MarkdownRenderer)
         - Create these as simple inline components or small separate files — they just fetch and render markdown, no structured cards needed for rollups
       - Right sidebar: only show summary metadata for daily views (when selectedType="daily"), show "Select a daily summary for details" for weekly/monthly
       - Pass `selectedDate` and `availableDates` to NavHeader (extracted from useSummaryList data)
  </action>
  <verify>
    cd web && pnpm build 2>&1 | tail -10
  </verify>
  <done>Left nav shows Daily/Weekly/Monthly groups. Clicking a date loads its summary. Prev/next arrows navigate between available dates. Calendar popover highlights available dates. Weekly/monthly entries render their markdown content.</done>
</task>

</tasks>

<verification>
1. `cd web && pnpm exec tsc --noEmit` — zero type errors
2. `cd web && pnpm build` — builds successfully
3. With `make dev`: left nav shows date list, clicking dates switches summary, prev/next works
4. Calendar popover opens and highlights available dates
5. Weekly/Monthly groups show roll-up entries (if roll-up files exist)
6. Empty groups are hidden
</verification>

<success_criteria>
- Left nav shows Daily, Weekly, Monthly expandable groups
- Each daily entry shows date, meeting count, commitment count
- Prev/next arrows skip to next available date
- Calendar popover highlights dates with summaries
- Missing dates show empty state in center panel
- Weekly and monthly entries are browsable from the same navigation
- Selection state is clearly visible (active item highlighted)
</success_criteria>

<output>
After completion, create `.planning/phases/25-next-js-scaffold-summary-view/25-04-SUMMARY.md`
</output>
