---
phase: 25-next-js-scaffold-summary-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/layout/app-shell.tsx
  - web/src/components/layout/sidebar-rail.tsx
  - web/src/components/layout/left-nav.tsx
  - web/src/components/layout/right-sidebar.tsx
  - web/src/app/page.tsx
  - web/src/app/globals.css
autonomous: true
requirements:
  - NAV-01

must_haves:
  truths:
    - "Opening localhost:3000 shows a three-column layout with left nav, center content, and right sidebar"
    - "Left sidebar collapses to a 48px icon rail when toggle is clicked"
    - "Right sidebar collapses to a 48px icon rail when toggle is clicked"
    - "When both sidebars are collapsed, center panel caps at 800-900px max-width and centers"
    - "A minimum viewport width is enforced (no mobile layout)"
  artifacts:
    - path: "web/src/components/layout/app-shell.tsx"
      provides: "Three-column CSS Grid container with dynamic column widths"
    - path: "web/src/components/layout/sidebar-rail.tsx"
      provides: "Collapsed sidebar icon rail component"
    - path: "web/src/components/layout/left-nav.tsx"
      provides: "Left nav panel shell with collapse toggle"
    - path: "web/src/components/layout/right-sidebar.tsx"
      provides: "Right sidebar panel shell with collapse toggle"
  key_links:
    - from: "web/src/components/layout/app-shell.tsx"
      to: "web/src/stores/ui-store.ts"
      via: "useUIStore for collapse state"
      pattern: "useUIStore"
    - from: "web/src/app/page.tsx"
      to: "web/src/components/layout/app-shell.tsx"
      via: "renders AppShell"
      pattern: "<AppShell"
---

<objective>
Build the three-column layout shell with collapsible sidebars.

Purpose: NAV-01 requires a three-column layout. This creates the structural foundation that all content components render into.
Output: Functional three-column layout with collapsible sidebars, visible at localhost:3000.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-next-js-scaffold-summary-view/25-RESEARCH.md
@web/src/app/layout.tsx
@web/src/app/globals.css

<interfaces>
From web/src/stores/ui-store.ts (created by Plan 01, but this plan can stub it):
```typescript
interface UIState {
  leftNavCollapsed: boolean;
  rightSidebarCollapsed: boolean;
  toggleLeftNav: () => void;
  toggleRightSidebar: () => void;
}
```

Note: This plan runs in Wave 1 alongside Plan 01. If ui-store.ts doesn't exist yet, create a minimal version inline. Plan 01 will write the full version. The executor should check if the file exists first and only create it if missing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three-column AppShell layout component</name>
  <files>
    web/src/components/layout/app-shell.tsx
    web/src/components/layout/sidebar-rail.tsx
    web/src/components/layout/left-nav.tsx
    web/src/components/layout/right-sidebar.tsx
  </files>
  <action>
    IMPORTANT: Read web/CLAUDE.md and follow its instructions (reference AGENTS.md about reading Next.js docs before writing code).

    1. Create `web/src/components/layout/app-shell.tsx` — marked "use client":
       - Import useUIStore (create stores/ui-store.ts first if it doesn't exist yet with just the collapse state)
       - CSS Grid layout: `grid h-screen` with three columns
       - Column widths: left = `280px` when expanded, `48px` when collapsed; center = `1fr`; right = `300px` when expanded, `48px` when collapsed
       - Use inline style for `gridTemplateColumns` driven by Zustand state (not Tailwind classes, because the values are dynamic)
       - Center panel: `overflow-y-auto` with `max-w-[900px] mx-auto` when BOTH sidebars are collapsed, full width otherwise
       - Children slots: `leftNav`, `children` (center), `rightSidebar` props
       - Minimum width: add `min-w-[1024px]` on the outer container

    2. Create `web/src/components/layout/sidebar-rail.tsx` — marked "use client":
       - Renders when a sidebar is collapsed
       - 48px wide column with vertical icon buttons
       - Left rail icons: Calendar icon (top), ChevronRight to expand
       - Right rail icons: Info icon (top), ChevronLeft to expand
       - Props: `side: "left" | "right"`, `onExpand: () => void`
       - Use Lucide icons: `Calendar`, `Info`, `ChevronLeft`, `ChevronRight`
       - Subtle border on the expanded side (border-r for left, border-l for right)
       - Icons stacked vertically with padding, muted color by default, hover highlight

    3. Create `web/src/components/layout/left-nav.tsx` — marked "use client":
       - Full left nav panel (when expanded)
       - Header with "Daily Summarizer" text and a ChevronLeft collapse button
       - Below header: placeholder content area (will be filled by Plan 03/04)
       - Props: `onCollapse: () => void`, `children?: ReactNode`
       - Styling: `h-full flex flex-col border-r bg-background`, header is `h-14 flex items-center px-4 border-b`
       - Overflow: body area is `flex-1 overflow-y-auto`

    4. Create `web/src/components/layout/right-sidebar.tsx` — marked "use client":
       - Full right sidebar panel (when expanded)
       - Header with "Summary Info" text and ChevronRight collapse button
       - Below header: placeholder content area (will be filled by Plan 03/04)
       - Props: `onCollapse: () => void`, `children?: ReactNode`
       - Styling: `h-full flex flex-col border-l bg-background`, header is `h-14 flex items-center px-4 border-b`
       - Overflow: body area is `flex-1 overflow-y-auto`
  </action>
  <verify>
    cd web && pnpm exec tsc --noEmit 2>&1 | head -20
  </verify>
  <done>Layout components type-check cleanly. AppShell renders three columns with proper widths.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppShell into page and update global styles</name>
  <files>
    web/src/app/page.tsx
    web/src/app/globals.css
  </files>
  <action>
    1. Update `web/src/app/globals.css`:
       - Keep existing Tailwind imports and CSS variables
       - Add: `html, body { min-width: 1024px; }` to enforce desktop minimum
       - Add scrollbar styling for webkit: thin, subtle (`.scrollbar-thin` utility class with `::-webkit-scrollbar` customization: 6px wide, rounded, muted track/thumb)

    2. Rewrite `web/src/app/page.tsx` — marked "use client":
       - Import AppShell, LeftNav, RightSidebar, SidebarRail from layout components
       - Import useUIStore
       - Render `<AppShell>` with:
         - leftNav: if collapsed -> `<SidebarRail side="left" onExpand={toggleLeftNav} />`, else -> `<LeftNav onCollapse={toggleLeftNav}><p className="p-4 text-muted-foreground">Date navigation coming soon</p></LeftNav>`
         - center children: `<div className="p-8"><h1 className="text-2xl font-semibold">Daily Summary</h1><p className="text-muted-foreground mt-2">Select a date from the left panel</p></div>`
         - rightSidebar: if collapsed -> `<SidebarRail side="right" onExpand={toggleRightSidebar} />`, else -> `<RightSidebar onCollapse={toggleRightSidebar}><p className="p-4 text-muted-foreground">Summary metadata coming soon</p></RightSidebar>`
       - Remove the old API status page content entirely
  </action>
  <verify>
    cd web && pnpm build 2>&1 | tail -5
  </verify>
  <done>localhost:3000 shows three-column layout. Clicking collapse buttons toggles sidebars to icon rails. Center content area visible.</done>
</task>

</tasks>

<verification>
1. `cd web && pnpm exec tsc --noEmit` — zero type errors
2. `cd web && pnpm build` — builds successfully
3. Visual: localhost:3000 shows three columns; left and right sidebars collapse to 48px rails; center content centers when both collapsed
</verification>

<success_criteria>
- Three-column layout renders at localhost:3000
- Left sidebar toggles between 280px expanded and 48px icon rail
- Right sidebar toggles between 300px expanded and 48px icon rail
- Center panel constrains to ~900px max-width when both sidebars collapsed
- Minimum viewport width of 1024px enforced
- Sidebar collapse state persists within session (via Zustand + sessionStorage)
</success_criteria>

<output>
After completion, create `.planning/phases/25-next-js-scaffold-summary-view/25-02-SUMMARY.md`
</output>
