---
phase: 28-pipeline-run-management
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - web/src/components/nav/nav-tab-switcher.tsx
  - web/src/stores/ui-store.ts
  - web/src/hooks/use-pipeline.ts
  - web/src/components/pipeline/run-history.tsx
  - web/src/components/layout/left-nav.tsx
autonomous: true
requirements: [PIPE-03]

must_haves:
  truths:
    - "Runs tab appears in left nav alongside Summaries and Entities"
    - "Runs tab shows table of past runs with status badge, duration, date, and timestamp"
    - "Default shows last 14 days of runs"
    - "Clicking a failed run expands to show error stage, message, and stack trace snippet"
    - "Clicking a successful run navigates to that date's summary"
  artifacts:
    - path: "web/src/components/pipeline/run-history.tsx"
      provides: "Run history table with expandable error details"
    - path: "web/src/components/nav/nav-tab-switcher.tsx"
      provides: "Updated tab switcher with runs tab"
    - path: "web/src/stores/ui-store.ts"
      provides: "Updated activeTab type to include 'runs'"
  key_links:
    - from: "web/src/components/pipeline/run-history.tsx"
      to: "/api/v1/runs"
      via: "TanStack Query useQuery"
      pattern: "useQuery.*runs"
    - from: "web/src/components/nav/nav-tab-switcher.tsx"
      to: "web/src/stores/ui-store.ts"
      via: "activeTab state"
      pattern: "activeTab.*runs"
    - from: "web/src/components/layout/left-nav.tsx"
      to: "web/src/components/pipeline/run-history.tsx"
      via: "conditional render when activeTab === 'runs'"
      pattern: "activeTab.*runs"
---

<objective>
Run history view -- Runs tab in left nav with past run listing and error drill-down

Purpose: Users can browse past pipeline runs to see what succeeded, what failed, and drill into failure details. Successful runs link directly to the corresponding summary.

Output: Runs tab in left nav, run history table, expandable failed run detail.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-pipeline-run-management/28-CONTEXT.md
@.planning/phases/28-pipeline-run-management/28-01-PLAN.md
@.planning/phases/28-pipeline-run-management/28-02-PLAN.md
@web/src/components/nav/nav-tab-switcher.tsx
@web/src/components/layout/left-nav.tsx
@web/src/stores/ui-store.ts

<interfaces>
<!-- From Plan 01 (API contract) -->
GET /api/v1/runs response:
```typescript
interface RunResponse {
  id: string;
  target_date: string;
  status: "running" | "complete" | "failed";
  stages: Array<{
    name: string;
    status: "pending" | "running" | "complete" | "failed";
    elapsed_s: number | null;
  }>;
  started_at: string;
  completed_at: string | null;
  duration_s: number | null;
  error_message: string | null;
  error_stage: string | null;
}
```

<!-- From Plan 02 (pipeline store) -->
From web/src/stores/pipeline-store.ts:
```typescript
interface PipelineState {
  runId: string | null;
  status: "idle" | "running" | "complete" | "failed";
  // ... rest of store
}
```

<!-- From existing nav pattern -->
From web/src/components/nav/nav-tab-switcher.tsx:
```typescript
const tabs = [
  { key: "summaries" as const, label: "Summaries" },
  { key: "entities" as const, label: "Entities" },
];
// activeTab type: "summaries" | "entities" from ui-store
```

From web/src/stores/ui-store.ts:
```typescript
activeTab: "summaries" | "entities";
setActiveTab: (tab: "summaries" | "entities") => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Runs tab to left nav</name>
  <files>web/src/stores/ui-store.ts, web/src/components/nav/nav-tab-switcher.tsx, web/src/components/layout/left-nav.tsx</files>
  <action>
    Update web/src/stores/ui-store.ts:
    - Change activeTab type from "summaries" | "entities" to "summaries" | "entities" | "runs"
    - Change setActiveTab parameter type accordingly
    - No other store changes needed

    Update web/src/components/nav/nav-tab-switcher.tsx:
    - Add third tab: { key: "runs" as const, label: "Runs" }
    - Tab styling stays the same -- the flex-1 approach will auto-size for 3 tabs

    Update web/src/components/layout/left-nav.tsx:
    - Import RunHistory component (created in Task 2)
    - Add conditional render: when activeTab === "runs", show RunHistory component instead of summary list or entity list
    - RunHistory component receives no props -- it fetches its own data
  </action>
  <verify>cd web && npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Left nav has three tabs: Summaries, Entities, Runs. Clicking Runs tab shows run history (placeholder until Task 2 wires it). TypeScript types are consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Run history table with expandable error details</name>
  <files>web/src/components/pipeline/run-history.tsx, web/src/hooks/use-pipeline.ts</files>
  <action>
    Add useRunHistory hook to web/src/hooks/use-pipeline.ts (append to existing file from Plan 02):
    - Uses TanStack Query: useQuery with queryKey ["pipeline-runs"], queryFn fetches GET /api/v1/runs?limit=14
    - Returns { data: RunResponse[], isLoading, error }
    - Refetch interval: 30 seconds (to pick up runs that complete while viewing history)

    Create web/src/components/pipeline/run-history.tsx:
    - "use client" component
    - Uses useRunHistory hook to fetch data
    - Loading state: 3 Skeleton rows (follow existing Skeleton pattern from other components)
    - Empty state: "No pipeline runs yet" with muted text
    - Table layout (not actual HTML table -- use div-based rows like existing entity list):
      - Each row: target_date (formatted "Apr 8"), status badge, duration (formatted "45.2s" or "--"), started_at (relative time like "2 hours ago" via date-fns formatDistanceToNow)
      - Status badges: green "Success" for complete, red "Failed" for failed, yellow pulsing "Running" for running
      - Use cn() for conditional styling (same pattern as entity-list-item.tsx)

    Click behavior:
    - Successful run: call setSelectedDate(run.target_date) and setActiveTab("summaries") from useUIStore to navigate to that summary
    - Failed run: toggle local expand state (useState) to show error detail panel below the row
    - Running run: no-op (or show "In progress..." text)

    Expanded error detail (for failed runs):
    - Show error_stage as "Failed at: {stage}" in red
    - Show error_message as preformatted text (font-mono, text-xs, max-h-32 overflow-auto)
    - Background: bg-destructive/10 rounded-md p-3
    - Click row again to collapse

    Style to match existing left nav content: same padding, font sizes, muted colors as date-list.tsx items.
  </action>
  <verify>cd web && npx tsc --noEmit 2>&1 | head -20 && echo "TypeScript check complete"</verify>
  <done>Run history table shows last 14 days of runs with status badges, duration, and timestamps. Failed runs expand to show error details. Successful runs navigate to the corresponding summary. Matches existing left nav visual style.</done>
</task>

</tasks>

<verification>
- Runs tab appears in left nav alongside Summaries and Entities
- Clicking Runs tab shows run history
- Status badges show correct colors for success/failed/running
- Clicking a successful run navigates to that date's summary
- Clicking a failed run expands error detail panel
- TypeScript compiles with no errors
</verification>

<success_criteria>
Users can view run history in the Runs tab, see status/duration/dates for each run, drill into failure details on failed runs, and navigate to summaries from successful runs.
</success_criteria>

<output>
After completion, create `.planning/phases/28-pipeline-run-management/28-03-SUMMARY.md`
</output>
