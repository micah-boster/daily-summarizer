---
phase: 27.1-verify-summary-view-fix-cmd-k-date-nav
plan: 02
type: execute
wave: 2
depends_on:
  - "27.1-01"
files_modified:
  - .planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md
autonomous: true
requirements:
  - SUM-01
  - SUM-02
  - SUM-04
  - NAV-01
  - NAV-05
  - UX-03
gap_closure: true

must_haves:
  truths:
    - "Daily summary view renders structured data + markdown for any available date (SUM-01)"
    - "Date navigation (prev/next, picker) works with graceful handling of missing dates (SUM-02)"
    - "Weekly and monthly roll-up summaries are browsable alongside daily summaries (SUM-04)"
    - "Three-column layout renders with left nav, center content, and right sidebar (NAV-01)"
    - "Cmd+K date selection navigates the center panel to the selected date (NAV-05)"
    - "Loading skeletons show per panel, error boundaries catch errors, toast notifications work (UX-03)"
  artifacts:
    - path: ".planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md"
      provides: "Formal verification report for all 6 requirements"
      contains: "status: passed"
  key_links:
    - from: "web/src/components/summary/summary-view.tsx"
      to: "web/src/hooks/use-summaries.ts"
      via: "useSummary hook"
      pattern: "useSummary"
    - from: "web/src/components/nav/nav-header.tsx"
      to: "web/src/app/page.tsx"
      via: "onSelectDaily/onPrev/onNext props"
      pattern: "onSelectDaily"
    - from: "web/src/components/command/command-palette.tsx"
      to: "web/src/stores/ui-store.ts"
      via: "setSelectedDate store action"
      pattern: "setSelectedDate"
    - from: "web/src/app/page.tsx"
      to: "web/src/components/layout/app-shell.tsx"
      via: "AppShell component with leftNav/rightSidebar slots"
      pattern: "AppShell"
---

<objective>
Verify all 6 Phase 27.1 requirements by static code analysis and produce a formal VERIFICATION.md report.

5 of these requirements (SUM-01, SUM-02, SUM-04, NAV-01, UX-03) are "orphaned" from Phase 25 -- the code exists but was never formally verified. This plan performs that verification. NAV-05 was partially implemented in Phase 27 and fixed in Plan 27.1-01 -- this plan verifies the fix works.

Purpose: Close the orphaned requirement gap from the v1.0 milestone audit. Every requirement gets a formal pass/fail with evidence.
Output: VERIFICATION.md file with pass/fail for all 6 requirements and human verification items where static analysis is insufficient.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-01-SUMMARY.md

Verification must check these code files exist and are wired correctly:
- web/src/components/summary/summary-view.tsx (SUM-01)
- web/src/components/nav/nav-header.tsx (SUM-02)
- web/src/components/nav/date-picker.tsx (SUM-02)
- web/src/app/page.tsx (SUM-04, NAV-01, NAV-05)
- web/src/components/layout/app-shell.tsx (NAV-01)
- web/src/components/layout/left-nav.tsx (NAV-01)
- web/src/components/layout/right-sidebar.tsx (NAV-01)
- web/src/components/command/command-palette.tsx (NAV-05)
- web/src/stores/ui-store.ts (NAV-05)
- web/src/components/ui/error-boundary.tsx (UX-03)
- web/src/components/ui/skeleton.tsx (UX-03)

IMPORTANT: Read web/CLAUDE.md and web/AGENTS.md before writing any Next.js code. Check node_modules/next/dist/docs/ for any relevant deprecation notices.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SUM-01, SUM-02, SUM-04 (Summary browsing requirements)</name>
  <files>.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md</files>
  <action>
  Verify three summary browsing requirements by reading source files and checking wiring:

  **SUM-01: Daily summary view for any available date**
  - Confirm `web/src/components/summary/summary-view.tsx` exists, accepts `selectedDate` prop, calls `useSummary(selectedDate)` hook
  - Confirm `web/src/hooks/use-summaries.ts` has `useSummary` hook that fetches from `/api/v1/summaries/{date}`
  - Confirm `web/src/app/page.tsx` renders `<SummaryView selectedDate={selectedDate} />` when `selectedType === "daily"`
  - Confirm `web/src/components/summary/structured-cards.tsx` renders structured sidecar data
  - Confirm `web/src/components/summary/markdown-renderer.tsx` renders markdown content
  - Check empty/error states exist in summary-view.tsx

  **SUM-02: Date navigation (prev/next, picker)**
  - Confirm `web/src/components/nav/nav-header.tsx` exists with prev/next navigation buttons
  - Confirm it computes `canGoPrev`/`canGoNext` from available dates list
  - Confirm `web/src/components/nav/date-picker.tsx` exists with calendar picker
  - Confirm date-picker calls `onSelectDate` callback with selected date string
  - Confirm `web/src/app/page.tsx` passes `handleSelectDaily` to left nav which reaches nav-header
  - Check graceful handling: what happens when no dates exist, or at boundary (first/last date)

  **SUM-04: Weekly and monthly roll-ups browsable**
  - Confirm `WeeklyView` and `MonthlyView` components exist in `web/src/app/page.tsx`
  - Confirm they use `useWeeklyRollup` and `useMonthlyRollup` hooks
  - Confirm `web/src/components/layout/left-nav.tsx` has weekly/monthly sections with selection handlers
  - Confirm `page.tsx` conditionally renders WeeklyView/MonthlyView based on `selectedType`

  Begin writing the VERIFICATION.md with status for each requirement (passed/failed + evidence lines).
  </action>
  <verify>
    <automated>test -f "/Users/micah/Desktop/CODE/DAILY SUMMARIZER/.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md" && grep -c "SUM-0" "/Users/micah/Desktop/CODE/DAILY SUMMARIZER/.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md"</automated>
  </verify>
  <done>VERIFICATION.md exists with pass/fail status and evidence for SUM-01, SUM-02, and SUM-04.</done>
</task>

<task type="auto">
  <name>Task 2: Verify NAV-01, NAV-05, UX-03 and finalize VERIFICATION.md</name>
  <files>.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md</files>
  <action>
  Continue verification for remaining three requirements:

  **NAV-01: Three-column layout**
  - Confirm `web/src/components/layout/app-shell.tsx` exists with three-column layout structure
  - Confirm it accepts `leftNav`, `rightSidebar`, and `children` (center) props/slots
  - Confirm `web/src/app/page.tsx` renders `<AppShell leftNav={...} rightSidebar={...}>{center}</AppShell>`
  - Confirm collapsible sidebars via `SidebarRail` components
  - Check CSS/Tailwind classes confirm three-column grid or flex layout

  **NAV-05: Command palette date navigation (post-fix)**
  - Confirm `web/src/stores/ui-store.ts` now has `selectedDate` state and `setSelectedDate` action
  - Confirm `web/src/components/command/command-palette.tsx` `handleSelectDate` calls `setSelectedDate(dateStr)`
  - Confirm `web/src/app/page.tsx` reads `selectedDate` from `useUIStore` (not from local useState)
  - Verify the full chain: user types date in Cmd+K -> handleSelectDate -> setSelectedDate (store) -> page.tsx reads store -> SummaryView renders date
  - Confirm `recentDates` still gets updated as well

  **UX-03: Loading skeletons, error boundaries, toasts**
  - Confirm `web/src/components/ui/error-boundary.tsx` exists with proper error boundary pattern
  - Confirm `web/src/app/page.tsx` wraps each column in `<ErrorBoundary>` (check for 3 wraps)
  - Confirm `web/src/components/ui/skeleton.tsx` exists (from shadcn)
  - Confirm skeleton usage: grep for `<Skeleton` in summary-view.tsx, page.tsx (WeeklyView/MonthlyView loading states)
  - Confirm toast: check for sonner or toast imports and usage (entity CRUD operations use toasts)

  Finalize the VERIFICATION.md with:
  - YAML frontmatter: phase, verified date, status (passed/failed), score
  - Per-requirement table with status and evidence
  - Human verification items for things static analysis cannot confirm (visual layout, keyboard interaction)
  - Key link verification table
  - Requirements coverage summary

  Follow the same format as `.planning/phases/27-entity-management-ui/27-VERIFICATION.md` for consistency.
  </action>
  <verify>
    <automated>grep -c "VERIFIED\|passed\|NAV-01\|NAV-05\|UX-03" "/Users/micah/Desktop/CODE/DAILY SUMMARIZER/.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md" | xargs test 6 -le</automated>
  </verify>
  <done>VERIFICATION.md is complete with all 6 requirements verified (passed/failed with evidence). Frontmatter has status and score. Human verification items listed for visual/interactive checks.</done>
</task>

</tasks>

<verification>
1. VERIFICATION.md exists: `test -f .planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md`
2. All 6 requirements are covered: `grep -c "SUM-01\|SUM-02\|SUM-04\|NAV-01\|NAV-05\|UX-03" .planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md`
3. Status is present: `grep "status:" .planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-VERIFICATION.md`
</verification>

<success_criteria>
- VERIFICATION.md exists with YAML frontmatter and structured verification report
- All 6 requirements (SUM-01, SUM-02, SUM-04, NAV-01, NAV-05, UX-03) have explicit pass/fail status with evidence
- NAV-05 shows the command palette -> store -> page.tsx chain is complete (from Plan 01 fix)
- Human verification items are listed for anything static analysis cannot confirm
- Report follows the same format as the Phase 27 VERIFICATION.md for consistency
</success_criteria>

<output>
After completion, create `.planning/phases/27.1-verify-summary-view-fix-cmd-k-date-nav/27.1-02-SUMMARY.md`
</output>
