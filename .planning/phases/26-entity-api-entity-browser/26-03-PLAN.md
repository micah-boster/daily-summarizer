---
phase: 26-entity-api-entity-browser
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - web/src/components/nav/nav-tab-switcher.tsx
  - web/src/components/nav/entity-filter-bar.tsx
  - web/src/components/nav/entity-list.tsx
  - web/src/components/nav/entity-list-item.tsx
  - web/src/components/layout/left-nav.tsx
  - web/src/components/entity/entity-scoped-view.tsx
  - web/src/components/entity/entity-header.tsx
  - web/src/components/entity/highlights-section.tsx
  - web/src/components/entity/commitments-section.tsx
  - web/src/components/entity/activity-timeline.tsx
  - web/src/components/entity/timeline-entry.tsx
  - web/src/components/entity/evidence-panel.tsx
  - web/src/components/layout/entity-sidebar.tsx
  - web/src/components/layout/right-sidebar.tsx
  - web/src/app/page.tsx
autonomous: false
requirements:
  - NAV-02
  - NAV-03
  - NAV-04
  - ENT-01
  - ENT-02
  - ENT-05
  - ENT-05

must_haves:
  truths:
    - "Left nav shows Summaries/Entities tab switcher, and switching tabs swaps the nav content"
    - "Entities tab shows entities grouped by type (Partners, People, Initiatives) with counts and expandable groups"
    - "Activity indicator green dot appears next to entities active in the last 7 days"
    - "Sort dropdown and type filter chips filter/sort the entity list"
    - "Clicking an entity in left nav loads its scoped view in the center panel"
    - "Entity scoped view shows header, highlights, open commitments, and activity timeline with significance badges"
    - "Right sidebar shows entity details (aliases, metadata, related entities) when an entity is selected"
    - "Right sidebar shows summary metadata when a summary is selected"
    - "Clicking a timeline entry expands to show source evidence with confidence badge"
    - "Evidence panel has a View in summary link that navigates to the daily summary for that date"
  artifacts:
    - path: "web/src/components/nav/nav-tab-switcher.tsx"
      provides: "Summaries/Entities segmented control"
    - path: "web/src/components/nav/entity-list.tsx"
      provides: "Entity list grouped by type with DateGroup pattern"
    - path: "web/src/components/entity/entity-scoped-view.tsx"
      provides: "Center panel entity view with all sections"
    - path: "web/src/components/entity/activity-timeline.tsx"
      provides: "Vertical timeline with expandable entries"
    - path: "web/src/components/entity/evidence-panel.tsx"
      provides: "Expanded source evidence with confidence badge"
    - path: "web/src/components/layout/entity-sidebar.tsx"
      provides: "Entity details for right sidebar"
    - path: "web/src/app/page.tsx"
      provides: "Updated page with tab switching and entity view routing"
  key_links:
    - from: "web/src/components/layout/left-nav.tsx"
      to: "web/src/stores/ui-store.ts"
      via: "activeTab state"
      pattern: "useUIStore.*activeTab"
    - from: "web/src/app/page.tsx"
      to: "web/src/hooks/use-entities.ts"
      via: "entity data hooks"
      pattern: "useEntityScopedView|useRelatedEntities"
    - from: "web/src/components/entity/evidence-panel.tsx"
      to: "web/src/stores/ui-store.ts"
      via: "View in summary cross-link"
      pattern: "setActiveTab.*summaries"
---

<objective>
Build the complete entity browser UI -- tab switcher in left nav, entity list with grouping/filtering/sorting, entity scoped view in center panel, context-aware right sidebar, and evidence drill-down.

Purpose: This is the user-facing deliverable of Phase 26. User sees entities, clicks one, sees its scoped view with contextual details, and can drill down into source evidence.
Output: Fully interactive entity browsing experience integrated into the three-column layout.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/26-entity-api-entity-browser/26-RESEARCH.md
@.planning/phases/26-entity-api-entity-browser/26-CONTEXT.md
@.planning/phases/26-entity-api-entity-browser/26-01-SUMMARY.md
@.planning/phases/26-entity-api-entity-browser/26-02-SUMMARY.md
@web/src/components/layout/left-nav.tsx
@web/src/components/layout/right-sidebar.tsx
@web/src/components/layout/app-shell.tsx
@web/src/components/nav/date-group.tsx
@web/src/components/nav/date-list.tsx
@web/src/components/summary/summary-view.tsx
@web/src/components/summary/structured-cards.tsx
@web/src/app/page.tsx
@web/src/stores/ui-store.ts
@web/src/hooks/use-entities.ts
@web/src/lib/types.ts

<interfaces>
From web/src/hooks/use-entities.ts (created in Plan 02):
```typescript
export function useEntityList(type?: string, sort?: string): UseQueryResult<EntityListItem[]>
export function useEntityScopedView(entityId: string | null): UseQueryResult<EntityScopedViewResponse>
export function useRelatedEntities(entityId: string | null): UseQueryResult<RelatedEntityItem[]>
```

From web/src/stores/ui-store.ts (extended in Plan 02):
```typescript
activeTab: "summaries" | "entities"
selectedEntityId: string | null
entityTypeFilter: string | null
entitySort: "activity" | "name"
setActiveTab: (tab) => void
selectEntity: (id) => void
setEntityTypeFilter: (type) => void
setEntitySort: (sort) => void
```

From web/src/lib/types.ts (added in Plan 02):
```typescript
interface EntityListItem { entity_id, name, entity_type, mention_count, commitment_count, last_active_date }
interface EntityScopedViewResponse { entity_name, entity_type, entity_id, aliases, highlights, open_commitments, activity_by_date, total_mentions }
interface ActivityItemResponse { source_type, source_date, context_snippet, confidence, significance_score }
interface RelatedEntityItem { entity_id, name, entity_type, co_mention_count }
```

From web/src/components/nav/date-group.tsx (pattern to reuse):
```typescript
// Collapsible group with icon, title, count, chevron toggle
export function DateGroup({ title, icon, count, groupId, children })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Left nav -- tab switcher, entity list, filter bar</name>
  <files>
    web/src/components/nav/nav-tab-switcher.tsx
    web/src/components/nav/entity-filter-bar.tsx
    web/src/components/nav/entity-list.tsx
    web/src/components/nav/entity-list-item.tsx
    web/src/components/layout/left-nav.tsx
  </files>
  <action>
1. Create `nav-tab-switcher.tsx` -- a segmented control with two tabs: "Summaries" and "Entities". Style as a compact inline segmented control (two adjacent buttons with shared border, active tab has bg-accent). Uses activeTab from useUIStore and setActiveTab. Place at the top of the left nav, below the header.

2. Create `entity-filter-bar.tsx` -- compact bar with:
   - Sort dropdown: "Activity" | "Name" (uses entitySort/setEntitySort from store). Use a simple select or button toggle, not a full dropdown component.
   - Type filter chips: "All" | "Partners" | "People" | "Initiatives" -- small pill buttons, active one gets bg-accent. Uses entityTypeFilter/setEntityTypeFilter from store. "All" sets filter to null.
   - Entire bar fits in ~40px height, always visible below tab switcher when Entities tab is active.

3. Create `entity-list-item.tsx` -- single entity item in the nav list:
   - Entity name (truncated if long)
   - Small green dot (h-2 w-2 rounded-full bg-green-500) if active in last 7 days (compare last_active_date to 7 days ago using date-fns differenceInDays)
   - Mention count as muted text on the right
   - Selected state: bg-accent styling
   - onClick calls selectEntity(entity.entity_id) and setActiveTab("entities")

4. Create `entity-list.tsx` -- groups entities by type using the DateGroup component pattern from Phase 25:
   - Three groups: "Partners" (Users icon), "People" (User icon), "Initiatives" (Lightbulb icon) from lucide-react
   - Each group shows count and is collapsible (reuse DateGroup or create similar pattern with expandedNavGroups from store)
   - If entityTypeFilter is set, only show the matching group
   - Loading skeleton when data is loading
   - Empty state when no entities exist

5. Update `left-nav.tsx`:
   - Add NavTabSwitcher between the header and scrollable body
   - When activeTab is "summaries": show existing NavHeader + date groups (current behavior)
   - When activeTab is "entities": show EntityFilterBar + EntityList
   - Keep the collapse button and header unchanged
   - Remove or hide the NavHeader (date picker) when in entities mode

Import useEntityList from hooks/use-entities and pass entityTypeFilter and entitySort from the store as parameters.
  </action>
  <verify>
    <automated>cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && npx tsc --noEmit 2>&1 | head -20 || echo "Type check done"</automated>
  </verify>
  <done>Left nav has working tab switcher. Entities tab shows grouped, filterable, sortable entity list with activity dots. Summaries tab shows existing date navigation unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Entity scoped view and right sidebar adaptation</name>
  <files>
    web/src/components/entity/entity-scoped-view.tsx
    web/src/components/entity/entity-header.tsx
    web/src/components/entity/highlights-section.tsx
    web/src/components/entity/commitments-section.tsx
    web/src/components/entity/activity-timeline.tsx
    web/src/components/entity/timeline-entry.tsx
    web/src/components/entity/evidence-panel.tsx
    web/src/components/layout/entity-sidebar.tsx
    web/src/components/layout/right-sidebar.tsx
    web/src/app/page.tsx
  </files>
  <action>
1. Create `entity-header.tsx` -- entity name as h1, entity type as muted badge below name. Shows aliases as small muted tag list under name (read-only, comma-separated or inline tags with bg-muted rounded-full px-2 py-0.5 text-xs styling).

2. Create `highlights-section.tsx` -- key stats row (total mentions, days active since first mention, last seen date) as compact stat boxes (reuse StatBox pattern from right-sidebar.tsx). Below stats, show top 2-3 highlights from the highlights array -- each as a brief card showing source_type, source_date, and truncated context_snippet.

3. Create `commitments-section.tsx` -- list of open commitments. Each shows source_date, context_snippet (truncated), and significance badge. If no commitments, show "No open commitments" muted text.

4. Create `timeline-entry.tsx` -- a single timeline item. Shows:
   - Source type icon: use lucide icons -- MessageSquare for "substance", Gavel for "decision", Handshake for "commitment" (or similar appropriate icons)
   - Source date formatted with date-fns
   - Brief description (truncated context_snippet, first ~80 chars)
   - Significance badge: "High" (bg-red-100 text-red-700), "Medium" (bg-yellow-100 text-yellow-700), "Low" (bg-gray-100 text-gray-700). Threshold: score >= 3.0 = High, >= 1.5 = Medium, else Low.
   - Expandable: clicking toggles local state to show/hide EvidencePanel. Use React useState, no global state needed.

5. Create `evidence-panel.tsx` -- expanded view shown inside timeline-entry:
   - Full context_snippet text
   - Source type icon + label
   - Date formatted
   - Confidence badge: percentage (Math.round(confidence * 100) + "%"), colored green (>80%), yellow (50-80%), red (<50%)
   - "View in summary" link at bottom. On click: calls setActiveTab("summaries") and the appropriate date selection handler (pass onViewInSummary callback from parent, which extracts source_date and calls handleSelectDaily).

6. Create `activity-timeline.tsx` -- vertical timeline grouped by day (using activity_by_date from the scoped view response). Newest first. Each day has a date header, then a list of TimelineEntry components. Style with a vertical line (border-l-2) connecting entries, with small dots at each entry.

7. Create `entity-scoped-view.tsx` -- orchestrator component for center panel:
   - Takes entityId prop
   - Uses useEntityScopedView(entityId) hook
   - Loading skeleton state (similar to summary view skeleton)
   - Error state / empty state for no data
   - Renders: EntityHeader, HighlightsSection, CommitmentsSection, ActivityTimeline in a scrollable container with padding
   - Pass a `onViewInSummary` callback that calls the parent's handleSelectDaily function

8. Create `entity-sidebar.tsx` -- entity details for right sidebar:
   - Entity type as uppercase label
   - Aliases as muted tag list (same as header, duplicated here for sidebar context)
   - Metadata section: show entity metadata as key-value pairs if available
   - Related entities section: use useRelatedEntities hook, show as clickable chips (bg-muted rounded-full px-2 py-1 text-xs cursor-pointer hover:bg-accent). Each chip shows entity name + co_mention_count in parentheses. Clicking a chip calls selectEntity(relatedEntity.entity_id).
   - Organization linkage: if entity has organization_id, display it (or "None" if null)

9. Update `right-sidebar.tsx`:
   - Accept new optional props: `entityId?: string | null`, `activeTab?: "summaries" | "entities"`
   - When activeTab is "entities" and entityId is set: render EntitySidebar instead of SummaryMetadata
   - Update header text: "Entity Details" when showing entity, "Summary Info" when showing summary
   - When activeTab is "entities" but no entityId: show "Select an entity to see details"

10. Update `page.tsx`:
    - Import useUIStore for activeTab, selectedEntityId, selectEntity, setActiveTab
    - Import useEntityScopedView, useRelatedEntities
    - Import EntityScopedView and EntitySidebar components
    - Center panel routing: when activeTab is "entities" and selectedEntityId is set, render EntityScopedView. Otherwise render existing summary/rollup views.
    - Right sidebar: pass activeTab and selectedEntityId (or entity data) to RightSidebar
    - Left nav: pass all entity-related store state and setters to LeftNav
    - "View in summary" handler: receives a date string, calls handleSelectDaily(date) and setActiveTab("summaries") to cross-link from entity evidence to daily summary
    - Keep ALL existing summary/rollup functionality unchanged
  </action>
  <verify>
    <automated>cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && npx tsc --noEmit 2>&1 | head -20 || echo "Type check done"</automated>
  </verify>
  <done>Entity scoped view renders in center panel with header, highlights, commitments, and expandable activity timeline. Right sidebar adapts to show entity details or summary metadata. Evidence drill-down works with confidence badges. "View in summary" cross-link navigates to daily summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of entity browser</name>
  <files>web/src/app/page.tsx</files>
  <action>
Human verifies the complete entity browser UI works end-to-end. All automated work is complete in Tasks 1-2.

What was built: Tab switcher in left nav, entity list grouped by type with activity dots and filter/sort, entity scoped view in center panel (header, highlights, commitments, activity timeline with significance badges), evidence drill-down with confidence scores, context-aware right sidebar showing entity details and related entity chips, "View in summary" cross-link.

How to verify:
1. Open http://localhost:3000 -- verify the three-column layout still works with Summaries tab selected (existing behavior unchanged)
2. Click "Entities" tab in left nav -- entity list appears grouped by Partners/People/Initiatives with counts
3. Verify green activity dots appear on recently active entities
4. Use sort dropdown (Activity/Name) and type filter chips -- list updates correctly
5. Click an entity -- center panel shows entity scoped view with header, highlights section, open commitments, and activity timeline
6. Check significance badges on timeline: High (red/orange), Medium (yellow), Low (gray)
7. Click a timeline entry -- it expands to show source evidence with confidence percentage badge (green/yellow/red)
8. Click "View in summary" in expanded evidence -- switches to Summaries tab and selects the correct date
9. Right sidebar shows entity details: type, aliases, metadata, related entity chips with co-mention counts
10. Click a related entity chip -- navigates to that entity's scoped view
11. Switch back to Summaries tab -- daily summary view works as before
  </action>
  <verify>Human visual verification of all 11 steps above</verify>
  <done>All entity browser UI flows verified: tab switching, entity list with grouping/filtering/sorting, scoped view, evidence drill-down, sidebar adaptation, and cross-linking all work correctly</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `cd web && npx tsc --noEmit`
- Dev server starts without errors: `cd web && npm run dev`
- Tab switcher toggles between Summaries and Entities content
- Entity list groups, filters, and sorts correctly
- Entity scoped view loads for any entity
- Timeline entries expand/collapse inline
- Confidence and significance badges display correct colors
- "View in summary" cross-link works end-to-end
- Right sidebar swaps between summary and entity modes
</verification>

<success_criteria>
- Complete entity browsing flow works end-to-end: browse list, select entity, see scoped view, drill into evidence
- Tab switcher feels native and doesn't lose state when switching
- Activity indicators, significance badges, and confidence badges display correctly
- Right sidebar is truly context-aware -- different content for summaries vs entities
- "View in summary" cross-link integrates entity browser with daily summaries
- All existing summary/rollup functionality unchanged
- All five success criteria from the roadmap are met
</success_criteria>

<output>
After completion, create `.planning/phases/26-entity-api-entity-browser/26-03-SUMMARY.md`
</output>
