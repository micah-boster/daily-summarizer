---
phase: 27-entity-management-ui
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/providers.tsx
autonomous: true
gap_closure: true
requirements:
  - ENT-03
  - ENT-04
  - NAV-05

must_haves:
  truths:
    - "EntityFormPanel renders when formPanelOpen is true in Zustand store"
    - "EntityDeleteDialog renders when deleteDialogOpen is true in Zustand store"
    - "Clicking Edit on entity header opens the slide-over form panel"
    - "Clicking Delete on entity header opens the delete confirmation dialog"
  artifacts:
    - path: "web/src/app/providers.tsx"
      provides: "Global mount point for EntityFormPanel and EntityDeleteDialog"
      contains: "EntityFormPanel"
  key_links:
    - from: "web/src/app/providers.tsx"
      to: "web/src/components/entity/entity-form-panel.tsx"
      via: "import and render"
      pattern: "EntityFormPanel"
    - from: "web/src/app/providers.tsx"
      to: "web/src/components/entity/entity-delete-dialog.tsx"
      via: "import and render"
      pattern: "EntityDeleteDialog"
    - from: "web/src/components/entity/entity-header.tsx"
      to: "web/src/stores/ui-store.ts"
      via: "openFormPanel / openDeleteDialog Zustand actions"
      pattern: "openFormPanel|openDeleteDialog"
---

<objective>
Mount orphaned EntityFormPanel and EntityDeleteDialog components in the global app tree so that entity create/edit and delete flows are functional end-to-end.

Purpose: Verification found that both components are fully implemented but never rendered -- the Zustand state updates (formPanelOpen, deleteDialogOpen) fire correctly from entity header buttons, but no Sheet or Dialog ever appears because the components are not in the React tree. This is the same mount pattern already used for CommandPalette in providers.tsx.

Output: Updated providers.tsx with both components mounted globally.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-entity-management-ui/27-VERIFICATION.md

<interfaces>
<!-- From web/src/app/providers.tsx (current state): -->
<!-- CommandPalette is already mounted after {children} inside QueryClientProvider -->
<!-- Pattern: import component, render alongside children -->

<!-- From web/src/components/entity/entity-form-panel.tsx: -->
<!-- export function EntityFormPanel() — self-contained Sheet that reads formPanelOpen from Zustand -->

<!-- From web/src/components/entity/entity-delete-dialog.tsx: -->
<!-- export function EntityDeleteDialog() — self-contained Dialog that reads deleteDialogOpen from Zustand -->

<!-- Both components manage their own open/close state via Zustand store slices. -->
<!-- No props needed — they read entityId and open state from useUIStore(). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount EntityFormPanel and EntityDeleteDialog in providers.tsx</name>
  <files>web/src/app/providers.tsx</files>
  <action>
Add two imports to web/src/app/providers.tsx:
- `import { EntityFormPanel } from "@/components/entity/entity-form-panel";`
- `import { EntityDeleteDialog } from "@/components/entity/entity-delete-dialog";`

Render both components inside the QueryClientProvider, after {children} and alongside the existing CommandPalette:

```tsx
<QueryClientProvider client={queryClient}>
  {children}
  <CommandPalette />
  <EntityFormPanel />
  <EntityDeleteDialog />
</QueryClientProvider>
```

This follows the exact same pattern as CommandPalette — globally mounted, self-contained components that read their open/close state from Zustand.

Do NOT modify any other file. The entity-header.tsx already calls openFormPanel() and openDeleteDialog() correctly. The components already read from Zustand and call the API. This is purely a mount fix.
  </action>
  <verify>
    <automated>cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && grep -c "EntityFormPanel\|EntityDeleteDialog" src/app/providers.tsx | grep -q "2" && echo "PASS: Both components referenced in providers.tsx" || echo "FAIL: Missing component mounts"</automated>
  </verify>
  <done>EntityFormPanel and EntityDeleteDialog are imported and rendered in providers.tsx. The Zustand-driven open/close flow now has a rendered target — clicking Edit or Delete on an entity header will show the corresponding overlay.</done>
</task>

</tasks>

<verification>
1. `grep "EntityFormPanel" web/src/app/providers.tsx` returns import and JSX lines
2. `grep "EntityDeleteDialog" web/src/app/providers.tsx` returns import and JSX lines
3. `cd web && npx next build 2>&1 | tail -5` completes without errors (TypeScript compilation validates imports)
</verification>

<success_criteria>
- Both EntityFormPanel and EntityDeleteDialog are imported and rendered in providers.tsx
- No other files modified
- TypeScript compiles without errors
- The entity create/edit slide-over and delete confirmation dialog are now reachable via UI interactions
</success_criteria>

<output>
After completion, create `.planning/phases/27-entity-management-ui/27-05-SUMMARY.md`
</output>
