---
phase: 27-entity-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/routers/entities.py
  - src/api/routers/merge_proposals.py
  - src/api/models/requests.py
  - src/api/models/responses.py
  - src/api/main.py
  - src/entity/repository.py
  - web/src/lib/api.ts
  - web/src/lib/types.ts
  - web/src/components/ui/sheet.tsx
  - web/src/components/ui/command.tsx
  - web/src/components/ui/dialog.tsx
  - web/src/components/ui/input.tsx
  - web/src/components/ui/select.tsx
  - web/src/components/ui/label.tsx
autonomous: true
requirements: [ENT-03, ENT-04, NAV-05]

must_haves:
  truths:
    - "POST /api/v1/entities creates a new entity and returns 201"
    - "PUT /api/v1/entities/{id} updates entity name/type and returns 200"
    - "DELETE /api/v1/entities/{id} soft-deletes entity and returns 204"
    - "POST /api/v1/entities/{id}/aliases adds alias and returns 201"
    - "DELETE /api/v1/entities/{id}/aliases/{alias} removes alias and returns 204"
    - "GET /api/v1/merge-proposals returns pending proposals with entity details"
    - "POST /api/v1/merge-proposals/{id}/approve executes merge and returns 200"
    - "POST /api/v1/merge-proposals/{id}/reject updates status and returns 200"
    - "apiMutate helper available in web/src/lib/api.ts for POST/PUT/DELETE"
    - "shadcn Sheet, Command, Dialog, Input, Select, Label components installed"
  artifacts:
    - path: "src/api/routers/entities.py"
      provides: "Entity CRUD + alias endpoints"
    - path: "src/api/routers/merge_proposals.py"
      provides: "Merge proposal list/approve/reject endpoints"
    - path: "src/api/models/requests.py"
      provides: "Pydantic request models for entity writes"
    - path: "web/src/lib/api.ts"
      provides: "apiMutate helper for write operations"
    - path: "web/src/lib/types.ts"
      provides: "TypeScript types for new API responses"
  key_links:
    - from: "src/api/routers/entities.py"
      to: "src/entity/repository.py"
      via: "Depends(get_entity_repo)"
      pattern: "repo\\.add_entity|repo\\.remove_entity|repo\\.add_alias|repo\\.remove_alias"
    - from: "src/api/routers/merge_proposals.py"
      to: "src/entity/merger.py"
      via: "generate_proposals, execute_merge"
      pattern: "generate_proposals|execute_merge"
---

<objective>
Add write API endpoints for entity CRUD, alias management, and merge proposal review. Install shadcn-ui components and extend the frontend API helper for mutations.

Purpose: All Phase 27 UI features depend on backend write endpoints and frontend infrastructure. This plan creates the complete backend + frontend foundation.
Output: Working API endpoints, request/response models, apiMutate helper, and shadcn-ui components ready for UI plans.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-entity-management-ui/27-RESEARCH.md
@src/api/routers/entities.py
@src/entity/repository.py
@src/entity/merger.py
@src/entity/models.py
@src/api/models/responses.py
@src/api/main.py
@web/src/lib/api.ts
@web/src/lib/types.ts

<interfaces>
<!-- Key existing interfaces the executor needs -->

From src/entity/repository.py:
- add_entity(name, entity_type, organization_id?, hubspot_id?, slack_channel?, metadata?) -> Entity
- remove_entity(entity_id) -> bool (soft-delete)
- add_alias(entity_id, alias) -> Alias
- remove_alias(alias) -> bool
- list_aliases(entity_id) -> list[Alias]
- get_by_id(entity_id) -> Entity | None

From src/entity/merger.py:
- generate_proposals(repo, entity_type?, limit?) -> list[dict] with source_entity, target_entity, score, source_context, target_context
- execute_merge(repo, source_id, target_id) exists but needs verification
- score_pair(name_a, name_b) -> float (0-100)

From src/entity/models.py:
- Entity(id, name, entity_type, organization_id, hubspot_id, slack_channel, metadata, merge_target_id, created_at, updated_at, deleted_at)
- Alias(id, entity_id, alias, created_at)
- MergeProposal(id, source_entity_id, target_entity_id, proposed_by, reason, status, created_at, resolved_at)
- EntityType: partner, person, initiative

From src/api/deps.py:
- get_entity_repo() -> EntityRepository (FastAPI dependency)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add entity write API endpoints and request models</name>
  <files>
    src/api/models/requests.py
    src/api/models/responses.py
    src/api/routers/entities.py
    src/entity/repository.py
  </files>
  <action>
1. Create src/api/models/requests.py with Pydantic request models:
   - CreateEntityRequest: name (str, required), entity_type (str, required, one of "partner"/"person"/"initiative")
   - UpdateEntityRequest: name (str | None), entity_type (str | None) -- partial update
   - AddAliasRequest: alias (str, required)

2. Add to src/api/models/responses.py:
   - EntityResponse: entity_id (str), name (str), entity_type (str) -- returned on create/update
   - AliasResponse: alias_id (str), entity_id (str), alias (str)

3. Add update_entity method to EntityRepository (repository.py currently has add_entity but no general update):
   ```python
   def update_entity(self, entity_id: str, name: str | None = None, entity_type: str | None = None) -> Entity | None:
   ```
   Use BEGIN IMMEDIATE for write safety per roadmap pitfall warning. Update only provided fields. Return updated entity or None if not found.

4. Add these endpoints to src/api/routers/entities.py:
   - POST /entities -- create entity using repo.add_entity, return 201 with EntityResponse
   - PUT /entities/{entity_id} -- update entity using repo.update_entity, return 200 with EntityResponse. 404 if not found.
   - DELETE /entities/{entity_id} -- soft-delete using repo.remove_entity, return 204. 404 if not found.
   - POST /entities/{entity_id}/aliases -- add alias using repo.add_alias, return 201 with AliasResponse. Handle IntegrityError as 409 with detail showing which entity owns the duplicate alias.
   - DELETE /entities/{entity_id}/aliases/{alias} -- remove alias using repo.remove_alias, return 204. 404 if alias not found.
   - GET /entities/unmatched-mentions -- query entity_mentions for names not matching any entity or alias, return list of strings. This supports alias autocomplete. Use a SQL query joining entity_mentions against entities and aliases to find unmatched source_id values. If this is too complex, return an empty list initially and add a TODO comment.

For all write endpoints, wrap the repository call in a try/except to catch sqlite3.OperationalError and return 503 with "Database busy" detail.
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && python -c "from src.api.routers.entities import router; print('Entity router OK')" && python -c "from src.api.models.requests import CreateEntityRequest, UpdateEntityRequest, AddAliasRequest; print('Request models OK')" && python -c "from src.api.models.responses import EntityResponse, AliasResponse; print('Response models OK')"
  </verify>
  <done>
    POST/PUT/DELETE entity endpoints exist and import successfully. CreateEntityRequest, UpdateEntityRequest, AddAliasRequest models defined. EntityRepository.update_entity method added. Alias add/remove endpoints handle duplicates gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add merge proposal API endpoints and register router</name>
  <files>
    src/api/routers/merge_proposals.py
    src/api/main.py
    src/api/models/responses.py
  </files>
  <action>
1. Add to src/api/models/responses.py:
   - MergeProposalResponse: proposal_id (str), source_entity (EntityListItem), target_entity (EntityListItem), score (float), reason (str | None), status (str), created_at (str)

2. Create src/api/routers/merge_proposals.py:
   - GET /merge-proposals -- list pending proposals. Use merger.generate_proposals(repo) to get fresh proposals (or query existing pending from DB). For each proposal, enrich with entity details (name, type, mention_count from repo.get_entity_stats). Return list of MergeProposalResponse.
   - POST /merge-proposals/{proposal_id}/approve -- Accept JSON body with primary_entity_id (the entity to keep). Call repo.update_proposal_status(proposal_id, "approved"). Then execute the merge: the non-primary entity gets merge_target_id set to primary, its mentions get reassigned, its name becomes an alias of the primary. Use the existing execute_merge function from merger.py (read it first to understand exact signature). Return 200 with updated proposal.
   - POST /merge-proposals/{proposal_id}/reject -- Call repo.update_proposal_status(proposal_id, "rejected"). Return 200 with updated proposal.

   Router prefix: /merge-proposals, tags: ["merge-proposals"]

3. Register the new router in src/api/main.py:
   ```python
   from src.api.routers.merge_proposals import router as merge_proposals_router
   app.include_router(merge_proposals_router, prefix="/api/v1")
   ```

Use BEGIN IMMEDIATE for all write operations. Wrap in try/except for sqlite3.OperationalError returning 503.
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && python -c "from src.api.routers.merge_proposals import router; print('Merge proposals router OK')" && python -c "from src.api.main import app; routes = [r.path for r in app.routes]; assert any('merge-proposals' in r for r in routes), f'merge-proposals not in routes: {routes}'; print('Router registered OK')"
  </verify>
  <done>
    Merge proposal list/approve/reject endpoints exist. Router registered in main app. Approve endpoint executes the actual merge (mention reassignment, alias transfer). Reject endpoint updates status only.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install shadcn-ui components and extend frontend API/types</name>
  <files>
    web/src/components/ui/sheet.tsx
    web/src/components/ui/command.tsx
    web/src/components/ui/dialog.tsx
    web/src/components/ui/input.tsx
    web/src/components/ui/select.tsx
    web/src/components/ui/label.tsx
    web/src/lib/api.ts
    web/src/lib/types.ts
  </files>
  <action>
1. Install shadcn-ui components. First read web/CLAUDE.md and web/AGENTS.md for any Next.js-specific guidance. Then:
   ```bash
   cd web && npx shadcn@latest add sheet command dialog input select label --yes
   ```
   If shadcn CLI prompts fail, manually create the component files following shadcn-ui patterns already established in the project (reference existing button.tsx, card.tsx patterns).

2. Add apiMutate helper to web/src/lib/api.ts:
   ```typescript
   export async function apiMutate<T>(
     path: string,
     options: { method: string; body?: unknown },
   ): Promise<T> {
     const res = await fetch(`${API_BASE}${path}`, {
       method: options.method,
       headers: { "Content-Type": "application/json" },
       ...(options.body ? { body: JSON.stringify(options.body) } : {}),
     });
     if (!res.ok) {
       const err = await res.json().catch(() => ({ detail: `HTTP ${res.status}` }));
       throw new Error(err.detail || `API error: ${res.status}`);
     }
     if (res.status === 204) return undefined as T;
     return res.json() as Promise<T>;
   }
   ```

3. Add TypeScript types to web/src/lib/types.ts:
   - CreateEntityPayload: { name: string; entity_type: string }
   - UpdateEntityPayload: { name?: string; entity_type?: string }
   - EntityResponse: { entity_id: string; name: string; entity_type: string }
   - AliasResponse: { alias_id: string; entity_id: string; alias: string }
   - MergeProposalResponse: { proposal_id: string; source_entity: EntityListItem; target_entity: EntityListItem; score: number; reason: string | null; status: string; created_at: string }

4. Verify Next.js build still passes:
   ```bash
   cd web && npm run build 2>&1 | tail -5
   ```
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && node -e "require('./src/components/ui/sheet.tsx')" 2>/dev/null; ls src/components/ui/sheet.tsx src/components/ui/command.tsx src/components/ui/dialog.tsx src/components/ui/input.tsx && echo "Components exist"
  </verify>
  <done>
    Sheet, Command, Dialog, Input, Select, Label components installed. apiMutate helper available for write operations. TypeScript types for all new API responses defined. Next.js build passes.
  </done>
</task>

</tasks>

<verification>
- All new endpoints return expected status codes (201/200/204/404/409/503)
- apiMutate properly handles error responses and 204 No Content
- shadcn-ui components render without build errors
- Existing entity list and scoped view endpoints still work (no regressions)
</verification>

<success_criteria>
- Entity CRUD endpoints (POST/PUT/DELETE) work with proper validation and error handling
- Alias add/remove endpoints work with duplicate detection
- Merge proposal list/approve/reject endpoints work with actual merge execution
- Frontend has apiMutate helper and all TypeScript types ready
- All shadcn-ui components for Phase 27 are installed
- No regressions in existing entity read endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/27-entity-management-ui/27-01-SUMMARY.md`
</output>
