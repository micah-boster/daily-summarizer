---
phase: 27-entity-management-ui
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/src/components/entity/entity-form-panel.tsx
  - web/src/components/entity/entity-delete-dialog.tsx
  - web/src/components/entity/entity-header.tsx
  - web/src/components/entity/entity-scoped-view.tsx
  - web/src/components/entity/alias-chip-list.tsx
  - web/src/components/entity/alias-input.tsx
  - web/src/stores/ui-store.ts
autonomous: true
requirements: [ENT-03]

must_haves:
  truths:
    - "User can open a slide-over panel to create a new entity with name and type fields"
    - "User can open the same panel pre-filled to edit an existing entity"
    - "Inline validation errors appear beneath fields when name is empty or type is missing"
    - "User can delete an entity by typing its name in a confirmation dialog"
    - "User can see alias chips below entity name in the scoped view"
    - "User can remove an alias by clicking X on its chip with undo toast"
    - "User can add a new alias via type-to-add input with autocomplete from unmatched mentions"
    - "Duplicate alias blocked with message showing which entity owns it"
  artifacts:
    - path: "web/src/components/entity/entity-form-panel.tsx"
      provides: "Slide-over create/edit form"
    - path: "web/src/components/entity/entity-delete-dialog.tsx"
      provides: "GitHub-style delete confirmation"
    - path: "web/src/components/entity/alias-chip-list.tsx"
      provides: "Alias tag/chip management"
    - path: "web/src/components/entity/alias-input.tsx"
      provides: "Autocomplete input for new aliases"
  key_links:
    - from: "web/src/components/entity/entity-form-panel.tsx"
      to: "/api/v1/entities"
      via: "apiMutate POST/PUT"
      pattern: "apiMutate.*entities"
    - from: "web/src/components/entity/entity-delete-dialog.tsx"
      to: "/api/v1/entities/{id}"
      via: "apiMutate DELETE"
      pattern: "apiMutate.*DELETE"
    - from: "web/src/components/entity/alias-chip-list.tsx"
      to: "/api/v1/entities/{id}/aliases"
      via: "apiMutate POST/DELETE"
      pattern: "apiMutate.*aliases"
---

<objective>
Build the entity CRUD form UI (slide-over panel for create/edit, delete confirmation dialog) and alias management UI (chip list with add/remove).

Purpose: Enables users to manage entities entirely from the browser -- the core of ENT-03.
Output: Working create/edit/delete entity flow and alias chip management in the entity scoped view.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-entity-management-ui/27-RESEARCH.md
@.planning/phases/27-entity-management-ui/27-01-SUMMARY.md
@web/src/components/entity/entity-header.tsx
@web/src/components/entity/entity-scoped-view.tsx
@web/src/stores/ui-store.ts
@web/src/lib/api.ts
@web/src/lib/types.ts
@web/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create entity form slide-over panel and delete confirmation dialog</name>
  <files>
    web/src/components/entity/entity-form-panel.tsx
    web/src/components/entity/entity-delete-dialog.tsx
    web/src/stores/ui-store.ts
  </files>
  <action>
IMPORTANT: Read web/CLAUDE.md and web/AGENTS.md first. Then read node_modules/next/dist/docs/ for any relevant Next.js guidance before writing components.

1. Add to Zustand store (ui-store.ts):
   - formPanelOpen: boolean (default false)
   - formPanelMode: "create" | "edit" (default "create")
   - formPanelEntityId: string | null (for edit mode)
   - openFormPanel: (mode, entityId?) => void
   - closeFormPanel: () => void
   - deleteDialogOpen: boolean (default false)
   - deleteDialogEntityId: string | null
   - openDeleteDialog: (entityId) => void
   - closeDeleteDialog: () => void

2. Create entity-form-panel.tsx:
   - Use shadcn Sheet component (side="right") for slide-over panel
   - Panel width: ~400px (reasonable for form, keeps entity list visible per user decision)
   - Form fields: name (Input, required), entity_type (Select with partner/person/initiative options, required)
   - In edit mode, pre-fill fields from entity data (fetch current entity from cache or API)
   - Inline validation: red text below field when name is empty on blur/submit, when type not selected
   - Submit button: "Create Entity" or "Save Changes" based on mode
   - On submit: call apiMutate POST or PUT, invalidate entity queries via queryClient, show success toast via sonner, close panel
   - On error: show error message from API response beneath form
   - Loading state: disable submit button, show spinner text during mutation

3. Create entity-delete-dialog.tsx:
   - Use shadcn Dialog component
   - Display entity name prominently: "Delete {entity.name}?"
   - Warning text: "This will soft-delete the entity and all its aliases. This action can be reversed by an administrator."
   - Input field: "Type the entity name to confirm"
   - Delete button: disabled until input matches entity name exactly (case-sensitive, GitHub-style)
   - Delete button: red/destructive variant
   - On confirm: call apiMutate DELETE, invalidate entity queries, show success toast, close dialog, navigate back to entity list
   - On error: show error message
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && npx tsc --noEmit 2>&1 | tail -10
  </verify>
  <done>
    Slide-over form panel opens for create/edit with name and type fields. Inline validation shows errors. Delete dialog requires typing entity name. Both components call API and invalidate cache on success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create alias chip list and autocomplete input, integrate into entity view</name>
  <files>
    web/src/components/entity/alias-chip-list.tsx
    web/src/components/entity/alias-input.tsx
    web/src/components/entity/entity-scoped-view.tsx
    web/src/components/entity/entity-header.tsx
  </files>
  <action>
IMPORTANT: Read web/CLAUDE.md and web/AGENTS.md first.

1. Create alias-chip-list.tsx:
   - Receive aliases: string[] and entityId: string as props
   - Render each alias as a Badge component (from existing ui/badge.tsx) with an X button
   - Color coding by entity type if desired (per user's Notion-style reference)
   - On X click: optimistically remove chip from UI, call apiMutate DELETE /entities/{entityId}/aliases/{alias}, show undo toast via sonner with "Undo" action button
   - Undo behavior: if user clicks Undo within toast duration (5 seconds), call apiMutate POST to re-add the alias, restore chip
   - If API delete fails: restore chip, show error toast

2. Create alias-input.tsx:
   - Render as a text input at the end of the chip list (Notion-style "Type to add")
   - On Enter or comma: submit the typed value as new alias via apiMutate POST /entities/{entityId}/aliases
   - Autocomplete dropdown: fetch unmatched mentions from GET /entities/unmatched-mentions (or use a simpler approach if that endpoint isn't ready -- filter locally against known entities/aliases)
   - Duplicate prevention: if API returns 409, show inline error "Already belongs to {entity name}" beneath the input
   - Clear input on successful add, invalidate entity scoped view query to refresh alias list
   - Keyboard: Escape clears input, Tab selects first autocomplete suggestion

3. Integrate into entity-scoped-view.tsx:
   - Import AliasChipList and AliasInput
   - Render below the entity name/header area
   - Pass aliases from the scoped view response and entityId

4. Add edit/delete buttons to entity-header.tsx:
   - Add a "Edit" button (pencil icon or text) that calls openFormPanel("edit", entityId)
   - Add a "Delete" button (trash icon, text-destructive) that calls openDeleteDialog(entityId)
   - These buttons should appear in the header area of the entity scoped view
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && npx tsc --noEmit 2>&1 | tail -10
  </verify>
  <done>
    Alias chips render below entity name with removable X buttons. Undo toast appears on removal. Type-to-add input with autocomplete works for new aliases. Duplicate aliases blocked with error message. Edit and delete buttons visible in entity header.
  </done>
</task>

</tasks>

<verification>
- Create entity flow: open panel, fill name+type, submit, see new entity in list
- Edit entity flow: click edit, see pre-filled form, change name, submit, see updated name
- Delete entity flow: click delete, type name to confirm, entity removed from list
- Add alias: type in input, press Enter, new chip appears
- Remove alias: click X on chip, chip disappears, undo toast appears
- Duplicate alias: type existing alias, see "Already belongs to..." error
</verification>

<success_criteria>
- Entity CRUD forms work end-to-end (create, edit, delete)
- Slide-over panel stays right-side, entity list remains visible
- Delete requires typing entity name (GitHub-style)
- Alias chips are removable with undo toast
- New aliases can be added with autocomplete
- Duplicate aliases are blocked with clear error message
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-entity-management-ui/27-02-SUMMARY.md`
</output>
