---
phase: 27-entity-management-ui
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - web/src/components/merge/merge-review-panel.tsx
  - web/src/components/merge/merge-comparison-card.tsx
  - web/src/components/merge/merge-primary-picker.tsx
  - web/src/stores/ui-store.ts
autonomous: true
requirements: [ENT-04]

must_haves:
  truths:
    - "User can see a list of pending merge proposals with similarity scores"
    - "User reviews proposals one at a time with auto-advance to next"
    - "Side-by-side cards show entity name, type, aliases, mention count, and similarity score"
    - "User picks which entity name becomes primary before approving"
    - "Approving a merge executes it immediately (mentions reassigned, non-primary becomes alias)"
    - "Rejecting skips to next proposal"
    - "Empty state shown when no pending proposals remain"
  artifacts:
    - path: "web/src/components/merge/merge-review-panel.tsx"
      provides: "Queue-based merge review flow"
    - path: "web/src/components/merge/merge-comparison-card.tsx"
      provides: "Side-by-side entity comparison cards"
    - path: "web/src/components/merge/merge-primary-picker.tsx"
      provides: "Radio selection for primary entity name"
  key_links:
    - from: "web/src/components/merge/merge-review-panel.tsx"
      to: "/api/v1/merge-proposals"
      via: "useQuery for list, useMutation for approve/reject"
      pattern: "apiMutate.*merge-proposals"
    - from: "web/src/components/merge/merge-review-panel.tsx"
      to: "web/src/components/merge/merge-comparison-card.tsx"
      via: "Props: source_entity, target_entity, score"
---

<objective>
Build the merge proposal review UI with side-by-side entity comparison, primary name selection, and one-at-a-time queue-based review flow.

Purpose: Enables users to review and act on merge proposals -- the core of ENT-04. Merging reduces duplicate entities and improves data quality.
Output: Working merge review page accessible from the entity tab, with approve/reject flow.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/27-entity-management-ui/27-RESEARCH.md
@.planning/phases/27-entity-management-ui/27-01-SUMMARY.md
@web/src/lib/api.ts
@web/src/lib/types.ts
@web/src/stores/ui-store.ts
@web/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create merge comparison card and primary picker components</name>
  <files>
    web/src/components/merge/merge-comparison-card.tsx
    web/src/components/merge/merge-primary-picker.tsx
  </files>
  <action>
IMPORTANT: Read web/CLAUDE.md and web/AGENTS.md first.

1. Create merge-comparison-card.tsx:
   - Props: entity (EntityListItem with mention_count), aliases (string[]), score (number), isSelected (boolean), onSelect (() => void)
   - Render as a Card component with:
     - Entity name (large, bold)
     - Entity type badge (partner/person/initiative)
     - Alias list (comma-separated or chips if few)
     - Mention count with label
     - Similarity score displayed as percentage (e.g., "87% similar") with colored indicator (green > 90, yellow 80-90, orange < 80)
   - Visual selection state: highlighted border or background when isSelected (user picks this as primary)
   - Clicking the card calls onSelect

2. Create merge-primary-picker.tsx:
   - Props: sourceEntity, targetEntity, selectedPrimaryId (string | null), onSelectPrimary ((entityId: string) => void)
   - Render two MergeComparisonCards side by side in a flex row with gap
   - Between cards: a "vs" divider or similarity score badge
   - Below cards: text "Select which name to keep as primary" when neither selected
   - When one is selected: the other card shows "Will become alias" label
   - This is a controlled component -- parent manages selectedPrimaryId state
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && npx tsc --noEmit 2>&1 | tail -10
  </verify>
  <done>
    Side-by-side comparison cards render entity details with similarity score. Primary picker lets user select which entity name to keep. Visual feedback shows selection state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create merge review panel with queue flow and integrate into entity tab</name>
  <files>
    web/src/components/merge/merge-review-panel.tsx
    web/src/stores/ui-store.ts
  </files>
  <action>
IMPORTANT: Read web/CLAUDE.md and web/AGENTS.md first.

1. Create merge-review-panel.tsx:
   - Fetch pending proposals with useQuery: GET /merge-proposals
   - Track current index in local state (starts at 0)
   - Display current proposal using MergePrimaryPicker
   - Progress indicator: "Reviewing 1 of 5 proposals" at top
   - Track selectedPrimaryId in local state (null until user picks)

   Action buttons at bottom:
   - "Approve Merge" button: disabled until primary selected. On click:
     - Call apiMutate POST /merge-proposals/{id}/approve with { primary_entity_id: selectedPrimaryId }
     - On success: show success toast, auto-advance to next proposal (increment index), reset selectedPrimaryId
     - Invalidate entity queries (merged entity list changed)
   - "Skip / Reject" button: always enabled. On click:
     - Call apiMutate POST /merge-proposals/{id}/reject
     - Auto-advance to next proposal
   - "Skip for Now" text button: advance without server call (review later)

   Empty state (no proposals or all reviewed):
   - Show a calm "All caught up" message with checkmark icon
   - "No merge proposals to review" if list was empty from start

   Loading state: skeleton cards while proposals load

2. Add to Zustand store (ui-store.ts):
   - Add to activeTab type: "summaries" | "entities" | "merge-review"
   - Or alternatively, add a showMergeReview boolean
   - Add a way to navigate to merge review from the entity tab (e.g., a "Review Merges" button in the entity list header area)

3. Integration point: The merge review panel should be accessible from the entities tab. Add a "Review Merges (N)" button/link in the entity list area or entity header that navigates to the merge review. Show the count of pending proposals as a badge.
  </action>
  <verify>
    cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER/web && export PATH="/Users/micah/.nvm/versions/node/v24.14.0/bin:$PATH" && npx tsc --noEmit 2>&1 | tail -10
  </verify>
  <done>
    Merge review panel shows one proposal at a time. User can select primary entity and approve, or reject/skip. Auto-advance to next proposal after action. Empty state shown when queue is empty. Accessible from entity tab with proposal count badge.
  </done>
</task>

</tasks>

<verification>
- Merge review accessible from entity tab area
- Proposals display with side-by-side comparison cards
- User can select primary entity by clicking a card
- Approve button disabled until primary selected
- After approve: merge executed, next proposal shown
- After reject: next proposal shown
- Empty state when all proposals reviewed
</verification>

<success_criteria>
- Side-by-side merge comparison cards show all required data (name, type, aliases, mentions, score)
- One-at-a-time review with auto-advance works smoothly
- User explicitly picks primary name before approving
- Approve executes the actual merge (not just status update)
- Calm, focused UX per user's design preference
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-entity-management-ui/27-03-SUMMARY.md`
</output>
