---
phase: 12-reliability-and-test-coverage
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - tests/test_pipeline.py
  - .github/workflows/ci.yml
autonomous: true
requirements: []

must_haves:
  truths:
    - Pipeline orchestration has test coverage for happy path (all sources succeed)
    - Pipeline orchestration has test coverage for single-source failure (Slack fails, others succeed)
    - Pipeline test verifies partial data synthesis when a source fails
    - GitHub Actions CI workflow runs pytest on push and PR
    - "uv run pytest" exits 0 with all tests passing
  artifacts:
    - tests/test_pipeline.py
    - .github/workflows/ci.yml
  key_links:
    - test_pipeline.py mocks all external dependencies (API clients, file I/O)
    - CI workflow uses uv for dependency management
---

<objective>
Add pipeline orchestration test coverage and GitHub Actions CI workflow.

Purpose: Ensure the pipeline runner logic is tested (happy path + failure degradation) and tests run automatically on every push.
Output: `tests/test_pipeline.py` with pipeline integration tests, `.github/workflows/ci.yml` for CI.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-reliability-and-test-coverage/12-CONTEXT.md
@.planning/phases/12-reliability-and-test-coverage/12-RESEARCH.md
@src/pipeline.py
@src/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pipeline orchestration tests</name>
  <files>tests/test_pipeline.py</files>
  <action>
Create `tests/test_pipeline.py` with comprehensive tests for `run_pipeline()` and the private ingest functions.

**Test infrastructure:**
- Create a `_make_pipeline_context()` helper that builds a `PipelineContext` with:
  - A minimal config dict (all sources disabled by default)
  - target_date = date(2026, 4, 1)
  - output_dir = tmp_path (pytest fixture)
  - template_dir = Path("templates")
  - A mock `claude_client` (Mock spec=anthropic.Anthropic)
  - google_creds = None (no Google by default)
  - calendar_service = None
  - gmail_service = None
- Use `@pytest.fixture` for tmp_path-based context creation

**Test class `TestIngestSlack`:**
1. `test_slack_disabled_returns_empty()` — config has slack.enabled=False, returns []
2. `test_slack_enabled_success()` — mock `fetch_slack_items` to return 2 SourceItems, verify 2 items returned
3. `test_slack_failure_returns_empty()` — mock `fetch_slack_items` to raise Exception, verify [] returned and warning logged

**Test class `TestIngestHubspot`:**
1. `test_hubspot_disabled_returns_empty()` — config has hubspot.enabled=False, returns []
2. `test_hubspot_failure_returns_empty()` — mock to raise, verify [] and warning

**Test class `TestIngestDocs`:**
1. `test_docs_disabled_returns_empty()` — config has google_docs.enabled=False
2. `test_docs_no_creds_returns_empty()` — enabled but google_creds=None

**Test class `TestRunPipeline`:**
1. `test_happy_path_no_sources()` — All sources disabled, pipeline runs to completion, writes a daily summary file to tmp_path. Mock `write_daily_summary` to return a Path, mock `write_daily_sidecar`, mock `send_slack_summary`. Verify pipeline completes without error.

2. `test_happy_path_with_slack()` — Enable Slack only. Mock `fetch_slack_items` to return items, mock `synthesize_daily` to return a result dict, mock writers. Verify `synthesize_daily` was called with the Slack items.

3. `test_single_source_failure_continues()` — Enable Slack + HubSpot. Mock Slack to raise Exception, mock HubSpot to return items. Verify pipeline still calls `synthesize_daily` with HubSpot items (slack_items=[]) and completes successfully. This is the key degradation test.

4. `test_synthesis_failure_still_writes()` — Mock `synthesize_daily` to raise Exception. Verify pipeline still calls `write_daily_summary` with empty synthesis result.

**Mocking strategy:**
- Use `unittest.mock.patch` to mock module-level imports in `src.pipeline`
- Mock at function level: `patch("src.pipeline.fetch_slack_items")`, `patch("src.pipeline.synthesize_daily")`, etc.
- Do NOT mock PipelineContext internals — pass real (minimal) config
- For `write_daily_summary`, mock to return `tmp_path / "output.md"` and create that file
- For file reads in notification (path.read_text), mock or create the file

Keep tests focused: each test verifies ONE behavior. Use descriptive assertion messages.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline.py -x -v</automated>
  </verify>
  <done>
    - tests/test_pipeline.py exists with 10+ tests
    - Happy path test verifies full pipeline flow
    - Single-source-failure test verifies degradation behavior
    - All pipeline tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GitHub Actions CI workflow and verify full test suite</name>
  <files>.github/workflows/ci.yml</files>
  <action>
1. Create `.github/workflows/ci.yml`:
```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v4
      - name: Set up Python
        run: uv python install 3.12
      - name: Install dependencies
        run: uv sync
      - name: Run tests
        run: uv run pytest -x -q
```

2. Run the FULL test suite locally to verify everything passes:
```bash
uv run pytest -x -q
```

If any test fails, diagnose and fix it. The goal is `uv run pytest` exits 0.

3. Verify test count: should be 318+ original tests + new tests from Plans 01 and 02 + pipeline tests from this plan.
  </action>
  <verify>
    <automated>uv run pytest -x -q</automated>
  </verify>
  <done>
    - `.github/workflows/ci.yml` exists and is valid YAML
    - `uv run pytest` exits 0 with ALL tests passing
    - Zero collection errors
    - Test count is 330+ (original 318 minus removed tests + all new tests)
  </done>
</task>

</tasks>

<verification>
- `uv run pytest -x -q` exits 0
- `uv run pytest --co -q 2>&1 | tail -3` shows 0 errors
- `ls .github/workflows/ci.yml` exists
- `uv run pytest tests/test_pipeline.py -v` shows happy path + failure path tests passing
</verification>

<success_criteria>
- Pipeline orchestration tested: happy path + single-source-failure + synthesis-failure
- GitHub Actions CI workflow configured for push + PR
- `uv run pytest` exits 0 — ALL tests pass, zero collection errors
- Total test count 330+
</success_criteria>

<output>
After completion, create `.planning/phases/12-reliability-and-test-coverage/12-03-SUMMARY.md`
</output>
