---
phase: 00-execution-model-validation
plan: 02
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - docs/re-auth-procedure.md
  - docs/cowork-setup.md
autonomous: false
requirements:
  - INGEST-01

must_haves:
  truths:
    - "A Cowork scheduled task fires reliably at configured time for 5 successful runs"
    - "A documented re-auth procedure exists for when tokens expire"
    - "Validation log shows 5 pass entries confirming execution model is sound"
  artifacts:
    - path: "docs/re-auth-procedure.md"
      provides: "Step-by-step OAuth re-authentication procedure"
      contains: "Re-Authentication Procedure"
    - path: "docs/cowork-setup.md"
      provides: "Cowork scheduled task configuration instructions"
      contains: "Cowork"
    - path: "output/validation/validation_log.jsonl"
      provides: "Validation log with 5+ pass entries"
      min_lines: 5
  key_links:
    - from: "Cowork scheduled task"
      to: "src/validation/daily_check.py"
      via: "Cowork executes the validation script daily at ~5-6pm ET"
      pattern: "daily_check"
    - from: "docs/re-auth-procedure.md"
      to: "src/auth/google_oauth.py"
      via: "Procedure references the auth module for re-authentication"
      pattern: "src.auth.google_oauth"
---

<objective>
Create operational documentation (Cowork setup guide and re-auth procedure), then guide the user through configuring the Cowork scheduled task and monitoring 5 successful validation runs. This plan completes Phase 0 by proving the execution model is reliable.

Purpose: The code from Plan 00-01 works in isolation. This plan proves it works as a scheduled, unattended daily pipeline -- the actual execution model the entire project depends on.
Output: Cowork scheduled task running daily, 5 successful validation runs logged, re-auth procedure documented.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-execution-model-validation/00-CONTEXT.md
@.planning/phases/00-execution-model-validation/00-RESEARCH.md
@.planning/phases/00-execution-model-validation/00-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create re-auth procedure and Cowork setup documentation</name>
  <files>
    docs/re-auth-procedure.md
    docs/cowork-setup.md
  </files>
  <action>
    **docs/re-auth-procedure.md:**
    Create the re-auth procedure document following the template in 00-RESEARCH.md "Re-auth Procedure Document Template". Include:
    - When re-auth is needed (Slack notification about refresh failure, RefreshError in logs, 7-day expiry if app still in Testing status)
    - Step-by-step: delete old token.json, run `uv run python -m src.auth.google_oauth`, sign in, grant permissions, verify refresh_token exists in token.json
    - Troubleshooting section: no refresh_token (delete token.json and re-run with prompt=consent), "Access blocked" (check test users in Google Cloud Console), 403 on API call (check scopes)
    - How to check current token status: `uv run python -c "from src.auth.google_oauth import load_credentials; c = load_credentials(); print(f'Valid: {c is not None}, Expiry: {c.expiry if c else \"N/A\"}')"`

    **docs/cowork-setup.md:**
    Create a Cowork scheduled task setup guide. Include:
    - What the scheduled task should do: "Run the daily validation script" with the command/prompt for Claude Code to execute: `cd "/Users/micah/Desktop/CODE/DAILY SUMMARIZER" && uv run python -m src.validation.daily_check`
    - Schedule: Daily at 5:00 PM ET (per locked decision: ~5-6pm ET, every day including weekends)
    - What to configure in Cowork: task name ("Daily Summarizer Validation"), schedule cadence (daily), time (5:00 PM ET), the instruction/prompt for the task
    - Expected behavior: Each run produces a timestamped JSON file in output/validation/ and appends to validation_log.jsonl
    - Machine requirements: Machine must be awake and Claude Desktop must be open at task time. If asleep, Cowork auto-runs on wake (per research Pitfall 2). Consider setting macOS energy schedule to prevent sleep during 5-6pm.
    - How to monitor: Check `output/validation/validation_log.jsonl` or run `uv run python -c "from src.validation.run_log import count_passes; print(f'Passes: {count_passes()}')"`
    - Success criteria: 5 total pass entries in the validation log (not necessarily consecutive days)
  </action>
  <verify>
    <automated>test -f "/Users/micah/Desktop/CODE/DAILY SUMMARIZER/docs/re-auth-procedure.md" && test -f "/Users/micah/Desktop/CODE/DAILY SUMMARIZER/docs/cowork-setup.md" && echo "Docs exist"</automated>
  </verify>
  <done>
    Both documentation files exist with actionable, step-by-step instructions. Re-auth procedure covers all failure modes from research. Cowork setup guide has exact schedule configuration.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Configure Cowork scheduled task and validate 5 successful runs</name>
  <files>
    output/validation/validation_log.jsonl
  </files>
  <action>
    User configures the Cowork scheduled task following docs/cowork-setup.md, then monitors runs over the next several days until 5 successful passes are logged.

    **Step 1: Set up Cowork scheduled task**
    Follow `docs/cowork-setup.md` to create a daily scheduled task in Cowork:
    - Task name: "Daily Summarizer Validation"
    - Schedule: Daily at 5:00 PM ET
    - Instruction: The task should execute the validation script in this project directory

    **Step 2: Monitor runs over the next several days**
    After each scheduled run, check:
    - New file in `output/validation/` directory
    - Slack DM notification received
    - Run: `cd "/Users/micah/Desktop/CODE/DAILY SUMMARIZER" && uv run python -c "from src.validation.run_log import count_passes; print(f'Passes: {count_passes()}')"`

    **Step 3: Validate completion (5 passes)**
    Once `count_passes()` returns 5 or more, Phase 0 is complete.

    Check the validation log for any failures:
    ```bash
    cat output/validation/validation_log.jsonl
    ```
    Review: Were there any failures? Did retry logic work? Did Slack notifications arrive for both successes and failures?

    **What success looks like:**
    - 5+ entries in validation_log.jsonl with status "pass"
    - Each entry has real calendar event data (not empty)
    - Slack DM notifications received for each run
    - No manual intervention was needed for OAuth refresh during the run period
  </action>
  <verify>User confirms 5+ pass entries in validation_log.jsonl and Slack notifications received consistently.</verify>
  <done>5 successful validation runs logged. Cowork scheduled task fires reliably. OAuth tokens auto-refresh. Slack notifications work. Phase 0 execution model is validated.</done>
</task>

</tasks>

<verification>
- docs/re-auth-procedure.md covers all failure modes (RefreshError, no refresh_token, scope mismatch)
- docs/cowork-setup.md has exact Cowork configuration details
- 5 successful validation runs logged in validation_log.jsonl
- Slack notifications received for each run
</verification>

<success_criteria>
- Cowork scheduled task fires daily at ~5pm ET
- 5 total pass entries in `output/validation/validation_log.jsonl`
- Each pass entry contains real calendar event data (titles, times, attendees)
- OAuth tokens auto-refreshed without manual intervention across the validation period
- Slack DM notifications received for successes (and failures if any occurred)
- Re-auth procedure document is accurate and actionable
</success_criteria>

<output>
After completion, create `.planning/phases/00-execution-model-validation/00-02-SUMMARY.md`
</output>
