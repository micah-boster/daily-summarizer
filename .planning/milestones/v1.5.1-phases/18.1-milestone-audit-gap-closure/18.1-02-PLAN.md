---
phase: 18.1-milestone-audit-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline.py
autonomous: true
requirements: [PERF-03, OPS-01, DEDUP-01]
gap_closure: true

must_haves:
  truths:
    - "No dead _ingest_calendar function exists in pipeline.py"
    - "No unused extract_all_meetings import exists in pipeline.py"
    - "All pipeline functionality still works (async pipeline unaffected)"
  artifacts:
    - path: "src/pipeline.py"
      provides: "Clean pipeline module with only live code"
      min_lines: 80
  key_links:
    - from: "src/pipeline_async.py"
      to: "src/pipeline.py"
      via: "imports PipelineContext, _ingest_docs, _ingest_hubspot, _ingest_notion, _ingest_slack"
      pattern: "from src\\.pipeline import"
    - from: "src/pipeline.py"
      to: "src/pipeline_async.py"
      via: "run_pipeline delegates to async_pipeline"
      pattern: "asyncio\\.run\\(async_pipeline"
---

<objective>
Remove dead sync pipeline code from pipeline.py -- the _ingest_calendar function and associated unused imports.

Purpose: Clean up tech debt identified in the v1.5.1 audit. Since Phase 17 moved all orchestration to pipeline_async.py, the sync _ingest_calendar function (lines 151-204) and several imports are unreachable dead code.
Output: Cleaned pipeline.py with only live code
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.5.1-MILESTONE-AUDIT.md

@src/pipeline.py
@src/pipeline_async.py

<interfaces>
<!-- What pipeline_async.py imports from pipeline.py (MUST be preserved): -->

From src/pipeline_async.py:
```python
from src.pipeline import (
    PipelineContext,
    _ingest_docs,
    _ingest_hubspot,
    _ingest_notion,
    _ingest_slack,
)
```

From src/main.py:
```python
from src.pipeline import PipelineContext, run_pipeline
```

<!-- These are the ONLY consumers of pipeline.py exports. Everything else is internal. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead _ingest_calendar and unused imports from pipeline.py</name>
  <files>src/pipeline.py</files>
  <action>
Remove the following dead code from `src/pipeline.py`:

**1. Remove the `_ingest_calendar` function (lines 151-204):**
This entire function is unreachable -- `run_pipeline()` delegates to `pipeline_async.py` which has its own `_ingest_calendar_async`.

**2. Remove unused imports that were only used by `_ingest_calendar`:**
After removing the function, the following imports become unused. Remove them:
- Line 21: `cache_raw_response`, `fetch_events_for_date` from `src.ingest.calendar` -- KEEP `build_calendar_service` ONLY IF used elsewhere (check: it is NOT used in remaining pipeline.py code, but pipeline_async.py imports it directly, so remove it)
- Line 22: `build_gmail_service`, `cache_raw_emails` from `src.ingest.gmail` -- remove entire line
- Line 26: `build_normalized_output` from `src.ingest.normalizer` -- remove entire line
- Line 29: `fetch_all_transcripts` from `src.ingest.transcripts` -- remove entire line
- Line 36: `extract_all_meetings` from `src.synthesis.extractor` -- remove entire line

**3. Also remove these imports that became unused when run_daily() was decomposed (pre-existing dead imports):**
- Line 21: entire `src.ingest.calendar` import line (all 3 names unused)
- Line 30: `DailySynthesis`, `Section` from `src.models.events` -- remove entire line
- Line 31: `SourceType` from `src.models.sources` -- remove but KEEP `SourceItem` (used by _ingest_* functions)
- Line 32: `send_slack_summary` from `src.notifications.slack` -- remove entire line
- Line 33: `write_daily_sidecar`, `write_daily_summary` from `src.output.writer` -- remove entire line
- Line 34: `detect_edits`, `save_raw_output`, `update_quality_report` from `src.quality` -- remove entire line
- Line 35: `extract_commitments` from `src.synthesis.commitments` -- remove entire line
- Line 37: `synthesize_daily` from `src.synthesis.synthesizer` -- remove entire line

**Imports that MUST remain (used by remaining functions):**
- `asyncio` (used by run_pipeline)
- `logging` (used by logger)
- `dataclass, field` from dataclasses (used by PipelineContext, IngestResult)
- `date, datetime, timedelta, timezone` from datetime (used by _ingest_slack)
- `Path` from pathlib (used by PipelineContext, _ingest_slack)
- `anthropic` (used by PipelineContext)
- `cleanup_raw_cache` from src.cache_cleanup (used by run_pipeline)
- `PipelineConfig, load_config` from src.config (used by PipelineContext)
- `fetch_google_docs_items` from src.ingest.google_docs (used by _ingest_docs)
- `fetch_hubspot_items` from src.ingest.hubspot (used by _ingest_hubspot)
- `fetch_notion_items` from src.ingest.notion (used by _ingest_notion)
- `fetch_slack_items, build_slack_client, check_new_channels, load_slack_state, save_slack_state` from src.ingest.slack + slack_discovery (used by _ingest_slack)
- `SourceItem` from src.models.sources (used by _ingest_* return types)

**Do NOT modify any function bodies** -- only remove dead code (the function + unused imports).

Verify that the resulting file still has: PipelineContext, IngestResult, _ingest_slack, _ingest_hubspot, _ingest_docs, _ingest_notion, run_pipeline.
  </action>
  <verify>
    <automated>cd "/Users/micah/Desktop/CODE/DAILY SUMMARIZER" && ! grep "_ingest_calendar" src/pipeline.py && ! grep "extract_all_meetings" src/pipeline.py && grep "def run_pipeline" src/pipeline.py && grep "class PipelineContext" src/pipeline.py && /Users/micah/.local/bin/uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>_ingest_calendar function removed, extract_all_meetings import removed, all other dead imports cleaned up, all remaining functions intact, all tests pass</done>
</task>

</tasks>

<verification>
1. `grep "_ingest_calendar" src/pipeline.py` returns no matches
2. `grep "extract_all_meetings" src/pipeline.py` returns no matches
3. `grep "def run_pipeline\|def _ingest_slack\|def _ingest_hubspot\|def _ingest_docs\|def _ingest_notion\|class PipelineContext" src/pipeline.py` returns 6 matches (all live code preserved)
4. `uv run pytest -x -q` passes (full test suite)
</verification>

<success_criteria>
- No dead _ingest_calendar function or extract_all_meetings import in pipeline.py
- All live functions (PipelineContext, IngestResult, 4 ingest functions, run_pipeline) preserved
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-milestone-audit-gap-closure/18.1-02-SUMMARY.md`
</output>
