---
phase: 13-typed-config-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/pipeline.py
  - src/main.py
  - src/ingest/calendar.py
  - src/ingest/drive.py
  - src/ingest/google_docs.py
  - src/ingest/hubspot.py
  - src/ingest/normalizer.py
  - src/ingest/slack.py
  - src/ingest/slack_discovery.py
  - src/ingest/transcripts.py
  - src/synthesis/commitments.py
  - src/synthesis/extractor.py
  - src/synthesis/synthesizer.py
  - src/synthesis/weekly.py
  - src/synthesis/monthly.py
  - tests/test_pipeline.py
  - tests/test_google_docs.py
  - tests/test_hubspot_ingest.py
autonomous: true
requirements:
  - CONFIG-01

must_haves:
  truths:
    - "Every config access in every source file uses typed attribute access (e.g., config.slack.channels), not .get() chains"
    - "PipelineContext.config is typed as PipelineConfig, not dict"
    - "Running the pipeline with a valid config.yaml produces identical output to the pre-migration behavior"
    - "All tests pass with typed config (no dict config fixtures remain)"
    - "Zero .get() calls for config access remain in src/ (excluding API response parsing which uses .get() on dicts that are NOT config)"
  artifacts:
    - path: "src/pipeline.py"
      provides: "PipelineContext with PipelineConfig type, typed config passing to all ingest/synthesis functions"
      contains: "config: PipelineConfig"
  key_links:
    - from: "src/pipeline.py"
      to: "all ingest and synthesis modules"
      via: "PipelineContext.config passes PipelineConfig to every function"
      pattern: "config\\.slack\\.|config\\.hubspot\\.|config\\.google_docs\\."
    - from: "tests/"
      to: "src/config.py"
      via: "make_test_config() factory replaces all dict config fixtures"
      pattern: "make_test_config"
---

<objective>
Migrate all config consumer modules from raw dict .get() access to typed PipelineConfig attribute access. Update PipelineContext.config type. Update all test fixtures to use make_test_config().

Purpose: Complete the typed config migration so every config access is validated at startup and IDE-navigable. Eliminates all .get() chains and dict-style config access.
Output: All 15 source files migrated, all 3 test files updated, zero dict config access remaining.
</objective>

<execution_context>
@/Users/micah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/micah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-typed-config-foundation/13-CONTEXT.md
@.planning/phases/13-typed-config-foundation/13-RESEARCH.md
@.planning/phases/13-typed-config-foundation/13-01-SUMMARY.md
@src/config.py
@src/pipeline.py

<interfaces>
<!-- From Plan 01: the PipelineConfig model tree -->
From src/config.py (created in Plan 01):
```python
class PipelineConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    pipeline: PipelineSettings
    calendars: CalendarsConfig
    transcripts: TranscriptsConfig
    synthesis: SynthesisConfig
    slack: SlackConfig
    google_docs: GoogleDocsConfig
    hubspot: HubSpotConfig

def load_config(config_path: Path | None = None) -> PipelineConfig: ...
def make_test_config(**overrides) -> PipelineConfig: ...
```

From src/pipeline.py (current -- will be changed):
```python
@dataclass
class PipelineContext:
    config: dict  # --> PipelineConfig
    target_date: date
    output_dir: Path
    template_dir: Path
    claude_client: anthropic.Anthropic
    google_creds: object | None = None
    calendar_service: object | None = None
    gmail_service: object | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate pipeline.py, main.py, and all synthesis modules</name>
  <files>src/pipeline.py, src/main.py, src/synthesis/extractor.py, src/synthesis/synthesizer.py, src/synthesis/commitments.py, src/synthesis/weekly.py, src/synthesis/monthly.py</files>
  <action>
**1. Update PipelineContext (src/pipeline.py):**
- Change `config: dict` to `config: PipelineConfig` on PipelineContext
- Add import: `from src.config import PipelineConfig`
- Migrate all 7 config access points in pipeline.py:
  - `ctx.config.get("slack", {}).get("enabled", False)` -> `ctx.config.slack.enabled`
  - `ctx.config.get("hubspot", {}).get("enabled", False)` -> `ctx.config.hubspot.enabled`
  - `ctx.config.get("google_docs", {}).get("enabled", False)` -> `ctx.config.google_docs.enabled`
  - `ctx.config.get("slack", {}).get("discovery_check_days", 7)` -> `ctx.config.slack.discovery_check_days`
  - And similar for any other .get() chains
- All `_ingest_*` functions already receive `ctx` -- they pass `ctx.config` to downstream functions

**2. Update main.py:**
- `load_config()` already returns PipelineConfig (from Plan 01)
- Migrate the 3 access points:
  - `config.get("pipeline", {}).get("output_dir", "output")` -> `config.pipeline.output_dir`
  - Similar for timezone if accessed here

**3. Update synthesis modules (5 files, ~15 access points total):**
Each synthesis module receives `config: dict` as a parameter. Update the type annotation and migrate .get() calls:

- **extractor.py** (3 access points): Update function signatures from `config: dict` to `config: PipelineConfig`. Migrate:
  - `config.get("synthesis", {}).get("model", ...)` -> `config.synthesis.model`
  - `config.get("synthesis", {}).get("extraction_max_output_tokens", 4096)` -> `config.synthesis.extraction_max_output_tokens`

- **synthesizer.py** (3 access points): Same pattern:
  - `config.get("synthesis", {}).get("model", ...)` -> `config.synthesis.model`
  - `config.get("synthesis", {}).get("synthesis_max_output_tokens", 8192)` -> `config.synthesis.synthesis_max_output_tokens`

- **commitments.py** (2 access points): Same pattern for synthesis.model access

- **weekly.py** (3 access points): Same pattern for synthesis.model and weekly_max_output_tokens

- **monthly.py** (3 access points): Same pattern for synthesis.model and monthly_max_output_tokens

For each file: add `from src.config import PipelineConfig` import, update function signature type hint, replace every `.get()` chain with attribute access. Per user decision: no hybrid access -- each file is fully converted.

**Anti-pattern to avoid:** Do NOT convert the model back to dict (no `.model_dump()`). Pass PipelineConfig directly.
  </action>
  <verify>
    <automated>cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER && uv run python -c "from src.pipeline import PipelineContext; import inspect; sig = inspect.signature(PipelineContext); print(f'config type: {sig.parameters[\"config\"].annotation}')"</automated>
  </verify>
  <done>
    - PipelineContext.config typed as PipelineConfig
    - All synthesis modules use typed attribute access
    - main.py uses typed attribute access
    - pipeline.py uses typed attribute access for enabled checks
    - Zero .get() config chains remain in these 7 files
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all ingest modules to typed config access</name>
  <files>src/ingest/calendar.py, src/ingest/drive.py, src/ingest/google_docs.py, src/ingest/hubspot.py, src/ingest/normalizer.py, src/ingest/slack.py, src/ingest/slack_discovery.py, src/ingest/transcripts.py</files>
  <action>
Migrate all 8 ingest modules from dict config access to typed PipelineConfig access.

**For each file:** Add `from src.config import PipelineConfig` (or the specific sub-model if the function only needs one section). Update function parameter type hints. Replace every `.get()` chain with attribute access.

**Per-file migration guide (research identified these access counts):**

- **src/ingest/slack.py** (10 access points -- highest):
  - Functions receive `config: dict` -> change to `config: PipelineConfig`
  - `config.get("slack", {}).get("enabled", False)` -> `config.slack.enabled`
  - `config.get("slack", {}).get("channels", [])` -> `config.slack.channels`
  - `config.get("slack", {}).get("dms", [])` -> `config.slack.dms`
  - `config.get("slack", {}).get("thread_min_replies", 3)` -> `config.slack.thread_min_replies`
  - `config.get("slack", {}).get("thread_min_participants", 2)` -> `config.slack.thread_min_participants`
  - `config.get("slack", {}).get("max_messages_per_channel", 100)` -> `config.slack.max_messages_per_channel`
  - `config.get("slack", {}).get("bot_allowlist", [])` -> `config.slack.bot_allowlist`
  - `config.get("pipeline", {}).get("timezone", "America/New_York")` -> `config.pipeline.timezone`
  - And similar for any other access patterns

- **src/ingest/hubspot.py** (10 access points):
  - `config.get("hubspot", {}).get("enabled", False)` -> `config.hubspot.enabled`
  - `config.get("hubspot", {}).get("ownership_scope", "mine")` -> `config.hubspot.ownership_scope`
  - `config.get("hubspot", {}).get("owner_id")` -> `config.hubspot.owner_id`
  - `config.get("hubspot", {}).get("max_deals", 50)` -> `config.hubspot.max_deals`
  - And similar for max_contacts, max_tickets, max_activities_per_type, portal_url

- **src/ingest/google_docs.py** (10 access points):
  - `config.get("google_docs", {}).get("enabled", False)` -> `config.google_docs.enabled`
  - `config.get("google_docs", {}).get("content_max_chars", 2500)` -> `config.google_docs.content_max_chars`
  - And similar for comment_max_chars, max_docs_per_day, exclude_ids, exclude_title_patterns

- **src/ingest/transcripts.py** (8 access points):
  - `config.get("transcripts", {}).get("gemini", {}).get("sender_patterns", [])` -> `config.transcripts.gemini.sender_patterns`
  - `config.get("transcripts", {}).get("gong", {}).get("sender_patterns", [])` -> `config.transcripts.gong.sender_patterns`
  - And similar for subject_patterns, preprocessing.strip_filler

- **src/ingest/calendar.py** (3 access points):
  - `config.get("calendars", {}).get("ids", ["primary"])` -> `config.calendars.ids`
  - `config.get("calendars", {}).get("exclude_patterns", [])` -> `config.calendars.exclude_patterns`
  - `config.get("pipeline", {}).get("timezone", ...)` -> `config.pipeline.timezone`

- **src/ingest/normalizer.py** (3 access points):
  - `config.get("transcripts", {}).get("matching", {}).get("time_window_minutes", 30)` -> `config.transcripts.matching.time_window_minutes`
  - `config.get("transcripts", {}).get("matching", {}).get("include_unmatched_events", True)` -> `config.transcripts.matching.include_unmatched_events`
  - `config.get("pipeline", {}).get("timezone", ...)` -> `config.pipeline.timezone`

- **src/ingest/drive.py** (3 access points):
  - `config.get("transcripts", {}).get("gemini_drive", {}).get("enabled", True)` -> `config.transcripts.gemini_drive.enabled`
  - `config.get("transcripts", {}).get("preprocessing", {}).get("strip_filler", True)` -> `config.transcripts.preprocessing.strip_filler`

- **src/ingest/slack_discovery.py** (3 access points):
  - `config.get("slack", {}).get("channels", [])` -> `config.slack.channels`
  - `config.get("slack", {}).get("dms", [])` -> `config.slack.dms`

**IMPORTANT:** Some `.get()` calls in these files are for parsing API responses (e.g., `event.get("summary")`, `message.get("text")`). Do NOT touch these -- only migrate `.get()` calls on the `config` parameter.

**Function signature strategy (per research recommendation):** Top-level functions (fetch_slack_items, fetch_hubspot_items, etc.) receive `PipelineConfig` since they access multiple sections (e.g., slack + pipeline.timezone). Internal helpers that only use one section can receive the sub-model, but for consistency, pass the root model unless there's a clear single-section helper.
  </action>
  <verify>
    <automated>cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER && grep -rn "config\.get\|config\[" src/ingest/ src/synthesis/ src/pipeline.py src/main.py --include="*.py" | grep -v "\.get(" | grep -v "# " | head -5; echo "---"; grep -c "config\.get" src/ingest/*.py src/synthesis/*.py src/pipeline.py src/main.py 2>/dev/null | grep -v ":0$"</automated>
  </verify>
  <done>
    - All 8 ingest modules use typed attribute access
    - No .get() chains for config access remain in any src/ file (API response .get() calls preserved)
    - All function signatures updated with PipelineConfig type hints
    - Pipeline runs identically with typed config
  </done>
</task>

<task type="auto">
  <name>Task 3: Update test fixtures and verify full test suite</name>
  <files>tests/test_pipeline.py, tests/test_google_docs.py, tests/test_hubspot_ingest.py</files>
  <action>
Migrate all test files that construct config dicts to use `make_test_config()` or direct PipelineConfig construction.

**1. tests/test_pipeline.py:**
- `_make_ctx()` currently builds a dict config. Change to use `make_test_config()`:
  ```python
  from src.config import PipelineConfig, make_test_config
  ```
- Update the default config construction to:
  ```python
  config = make_test_config(
      slack={"enabled": True, "channels": ["C123"]},
      hubspot={"enabled": True},
      google_docs={"enabled": True},
  )
  ```
- Update any test that passes config overrides to use the typed model
- PipelineContext now expects `config: PipelineConfig` so plain dicts will fail

**2. tests/test_google_docs.py:**
- Has `_base_config()` helper returning a dict. Replace with `make_test_config(google_docs={...})`:
  ```python
  from src.config import make_test_config
  ```
- Wherever tests pass config dicts to `fetch_google_docs_items()`, use `make_test_config()` instead
- The function now expects PipelineConfig, not dict

**3. tests/test_hubspot_ingest.py:**
- Similar pattern: replace any dict config construction with `make_test_config(hubspot={...})`
- Functions now expect PipelineConfig

**4. Run full test suite:**
After migration, run:
```
uv run pytest tests/ -x --ignore=tests/test_notifications.py --ignore=tests/test_source_models.py -v
```
The two pre-existing broken tests (test_notifications, test_source_models) are excluded as they were known-broken before this phase.

**5. Final verification -- zero dict config access remaining:**
```
grep -rn "config\.get\|config\[" src/ --include="*.py" | grep -v "\.get(" | grep -v "response\|event\|message\|item\|data\|result\|body\|payload\|headers"
```
This grep should return zero lines (all remaining .get() calls should be on API response objects, not config).
  </action>
  <verify>
    <automated>cd /Users/micah/Desktop/CODE/DAILY\ SUMMARIZER && uv run pytest tests/ -x --ignore=tests/test_notifications.py --ignore=tests/test_source_models.py -q</automated>
  </verify>
  <done>
    - All test fixtures migrated from dict config to PipelineConfig/make_test_config()
    - All tests pass (excluding 2 pre-existing broken tests)
    - Zero dict-style config access remains in src/
    - Pipeline produces identical output with typed config
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/ -x --ignore=tests/test_notifications.py --ignore=tests/test_source_models.py -v` -- all pass
- `grep -rn "config\.get(" src/ingest/ src/synthesis/ src/pipeline.py src/main.py --include="*.py" | grep -v "event\.\|message\.\|item\.\|data\.\|result\.\|response\.\|body\.\|payload\.\|headers\." | wc -l` -- returns 0 (no config .get() chains remain)
- `grep -rn "PipelineConfig" src/pipeline.py` -- confirms PipelineContext uses PipelineConfig type
- `grep -rn "make_test_config" tests/` -- confirms test fixtures use the factory
</verification>

<success_criteria>
- All 15 source files migrated from dict to typed config access
- PipelineContext.config is PipelineConfig, not dict
- All tests pass with typed config
- No .get() chains for config access remain in src/
- Pipeline output identical to pre-migration
</success_criteria>

<output>
After completion, create `.planning/phases/13-typed-config-foundation/13-02-SUMMARY.md`
</output>
